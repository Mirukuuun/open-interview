# AGENTS Workflow Router Plan

[X] task1. 阅读 `AGENTS.md`、`.codex/workflows/coding.md`、`.codex/workflows/init.md`、L1 总览与相关 rules，确认当前 workflow 与导航口径。
[X] task2. 更新 `AGENTS.md`，新增 `ROUTER` 表格，明确不同任务类型命中的 workflow、必读时机与非必读场景。
[X] task3. 回看文档索引与 L2 回环要求，确认本次改动无需同步更新 `.codex/context/*` 等上下文文档。
[X] task4. 运行同构检查并完成本次文档改动自检。
