[X] task1. 阅读 AGENTS、coding workflow、相关 L1/L2 context、rules 与产品 review 执行文档，确认 A-F 切片范围与交付要求。
[X] task2. 创建并维护本次实现计划，启用仓库 hook，确认共享入口与脏工作树边界。
[X] task3. 实现 workspace summary、首页动态分流、sidebar 审核 badge 与统一时间格式工具。
[X] task4. 实现导入页空态引导、QA markdown 渲染、题库详情到 QA 的 query prefill 入口。
[X] task5. 实现 SQLite 备份脚本、题库 JSON 导出脚本与恢复说明文档。
[X] task6. 实现 practice 定向练习入口、过滤展示与退出过滤。
[X] task7. 实现面经列表按公司浏览/分组与沉淀状态强化，提升详情页首屏 CTA 与状态反馈。
[X] task8. 实现 resume landing 去 placeholder 化、coach hints 强化与项目亮点结构化展示增强。
[X] task9. 更新相关 L2/context、`docs/ui-flows.md` 与必要的使用/恢复文档，完成文档回环。
[X] task10. 执行同构检查、`db:init`、`typecheck`、`lint`、`build` 并修复问题。
[ ] task11. 在当前分支执行 `commit` 与 `push`，确保远端包含本次改动。
[ ] task12. 执行 `corepack pnpm deploy:mvp` 并回归验证 `/import`、`/qa`、`/api/health`。
