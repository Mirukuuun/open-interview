[X] task1. 读取当前 `.gitignore`、相关 Plan 与 git 状态，确认 `volumes/` 已被 ignore 但仍处于已跟踪状态
[X] task2. 将 `volumes/` 下的运行时文件从 Git 索引中移除，保留本地副本
[X] task3. 校验工作区状态，确认后续新增运行时文件不会再次进入版本控制
[ ] task4. 提交并推送本次清理改动，同时说明历史中已存在的大文件仍保留在旧 commit 中
