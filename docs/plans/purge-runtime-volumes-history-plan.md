[X] task1. 检查当前分支、工作区状态与历史对象，确认 `volumes/` 与早期顶层 `etcd` / `milvus` / `minio` 目录仍存在于当前分支历史中
[X] task2. 创建本地备份分支，保留改写前的引用以便回退
[ ] task3. 仅针对当前分支执行历史改写，移除 `volumes/`、`etcd/`、`milvus/`、`minio/` 路径的全部历史对象
[ ] task4. 校验当前分支历史已不再包含这些运行时目录对象，并说明本地文件仍保留
[ ] task5. 强推改写后的分支到远端，提示旧 commit hash 将失效
