# Runtime Artifacts Gitignore Plan

[X] task1. 阅读 `AGENTS.md`、`.codex/workflows/coding.md`、现有 `.gitignore` 与当前 git 状态，确认需要忽略的运行时目录范围。
[X] task2. 为本次任务创建 Plan 文档，并记录执行中的任务项与完成状态。
[X] task3. 更新 `.gitignore`，加入 `etcd`、`milvus`、`minio`、`volumes` 等本地运行时数据目录的忽略规则。
[X] task4. 验证忽略规则是否覆盖目标目录，并说明已跟踪文件不会仅因 `.gitignore` 自动停止跟踪。
