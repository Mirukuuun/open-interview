---
category: development
files_owned:
  - src/app/globals.css
files_readonly:
  - docs/001-webui-refactor/design.md
  - docs/001-webui-refactor/roadmap.md
  - components.json
files_shared: []
---

# T1.1 · Tokens 层完全重写 + 废弃 CSS 清理

## 目标

重写 `src/app/globals.css`，建立符合 `design.md` §5 的完整 tokens 体系（颜色 / 字体 / 字号 / 圆角 / 阴影 / 表面层级 / 间距 / 动效），并砍掉所有废弃资源（radial gradient / interactive-card / reveal-list / fade-in-up / radar-draw / sidebar-tooltip / surface-nav / selection 靛紫）。为浅色 + 深色双态同时定义 tokens。

## 涉及文件

- **Modify**: `src/app/globals.css`（完全重写）
- **Test**: 无独立 Vitest 测试；以 `corepack pnpm build` 成功 + Tailwind v4 @theme 编译无错误为通过判定

## 验收条件

- [ ] `globals.css` 中定义以下 tokens（浅色默认 + `.dark` 深色变体）：
  - 功能色：`--color-brand / --color-brand-soft / --color-success / --color-success-soft / --color-warning / --color-warning-soft / --color-destructive / --color-destructive-soft / --color-info / --color-info-soft`
  - 中性灰阶语义别名：`--color-background / --color-foreground / --color-border / --color-muted-foreground`
  - 表面层级：`--color-surface / --color-surface-muted / --color-surface-subtle`
  - 圆角：`--radius-sm: 4px / --radius-md: 6px / --radius-lg: 10px / --radius-full: 999px`
  - 阴影：`--shadow-sm / --shadow-md / --shadow-lg`（design.md §5.6 给的 rgba 值）
  - 字体栈：`--font-sans: "Inter", "PingFang SC", "Microsoft YaHei", sans-serif; --font-mono: "JetBrains Mono", ui-monospace, monospace;`
  - timing：`--duration-base: 150ms; --ease-out: cubic-bezier(0, 0, 0.2, 1);`
- [ ] `@theme inline { ... }` 映射到 Tailwind v4 工具类（color / radius / shadow / font / transition-duration / transition-timing-function）
- [ ] `.dark` 深色根类下完整覆盖所有 color / surface tokens（背景 #09090B/#18181B、foreground #FAFAFA、border #27272A、muted-fg #A1A1AA、brand #3B82F6，soft 用 rgba 透明叠加）
- [ ] **砍掉**以下代码（git diff 可见删除，不是覆盖）：
  - `:root` 下：`--surface-nav` / `--surface-nav-muted` / `--border-strong` / `--border-muted` / `--text-strong` / `--text-muted` / `--text-inverse` / `--accent` / `--accent-secondary` / `--accent-soft` / `--success-soft` / `--warning` / 现有 `--radius-sm/md/lg/xl`（14/18/24/28）
  - `body` 的 radial gradient + linear-gradient 背景
  - `::selection` 的 `rgba(99, 102, 241, 0.22)` 靛紫高亮（改为 brand soft）
  - `.interactive-card` 全部规则块（hover / focus-visible）
  - `.sidebar-tooltip` 全部规则块 + `:hover > .sidebar-tooltip` / `:focus-visible` / `:focus-within` 伪类
  - `.reveal-list > *` 和 `:nth-child(1..6)` 所有规则
  - `@keyframes fade-in-up`
  - `@keyframes radar-draw`
  - 保留 `@keyframes skeleton-pulse`
  - 保留 `@media (prefers-reduced-motion: reduce)` 块
- [ ] `html { color-scheme: light dark; }`（让原生控件跟随 .dark 切换）
- [ ] `@import "@fontsource/inter/..."` 替换原 `@fontsource/ibm-plex-sans/...`（具体 weight 引入由 T1.2 在 layout.tsx 决定；本 task 仅调整 globals.css 字体栈变量值）
- [ ] `corepack pnpm build` 成功（无 Tailwind v4 @theme 编译错误）

## 依赖

- 前置：无（是本 phase 的首 task）
- 后置：所有其他 T1.X 都需要本 task 产出的 tokens

## 估算

0.5–0.75 天（约 4–6 小时）

## 要动的文件

- Modify: `src/app/globals.css`（完全重写，约 216 行 → 预计 180–220 行）

## Step 1 · 写失败测试

本 task 为 CSS / tokens 类重写，不适合独立 Vitest；使用构建命令作为"测试"。准备验证命令：

```bash
corepack pnpm build 2>&1 | tee /tmp/t1.1-build.log
grep -E "(surface-nav|interactive-card|reveal-list|fade-in-up|radar-draw|sidebar-tooltip)" src/app/globals.css || echo "CLEAN: all deprecated tokens removed"
grep -c "@fontsource/ibm-plex" src/app/globals.css && echo "FAIL: IBM Plex still imported" || echo "CLEAN: IBM Plex removed"
grep -c "\.dark" src/app/globals.css  # 应 ≥ 1
```

## Step 2 · run 测试确认 FAIL

在重写前执行上述命令集。Expected：
- 构建原样通过（无红线）
- 第二条 grep 命中 6 处废弃 token（当前状态）
- 第三条 grep 命中 `@fontsource/ibm-plex-sans/*.css` 4 行（当前状态）
- 第四条 grep 返回 0（当前无 `.dark` 支持）

FAIL 判据：有 deprecated token 残留 + 无 `.dark` 支持。

## Step 3 · 写最小实现

重写 `src/app/globals.css`。建议结构：

```css
@import "@fontsource/inter/400.css";
@import "@fontsource/inter/500.css";
@import "@fontsource/inter/600.css";
@import "@fontsource/inter/700.css";
@import "@fontsource/jetbrains-mono/400.css";
@import "@fontsource/jetbrains-mono/500.css";
@import "@fontsource/jetbrains-mono/700.css";
@import "tailwindcss";

@custom-variant dark (&:where(.dark, .dark *));

:root {
  /* typography */
  --font-sans: "Inter", "PingFang SC", "Microsoft YaHei", sans-serif;
  --font-mono: "JetBrains Mono", ui-monospace, SFMono-Regular, monospace;

  /* neutrals (zinc 9 档语义别名) */
  --color-background: #fafafa;
  --color-foreground: #09090b;
  --color-border: #e4e4e7;
  --color-muted-foreground: #71717a;

  /* surfaces */
  --color-surface: #ffffff;
  --color-surface-muted: #fafafa;
  --color-surface-subtle: #f4f4f5;

  /* functional colors */
  --color-brand: #2563eb;
  --color-brand-soft: #eff6ff;
  --color-success: #059669;
  --color-success-soft: #ecfdf5;
  --color-warning: #d97706;
  --color-warning-soft: #fffbeb;
  --color-destructive: #dc2626;
  --color-destructive-soft: #fef2f2;
  --color-info: #0284c7;
  --color-info-soft: #f0f9ff;

  /* radii */
  --radius-sm: 4px;
  --radius-md: 6px;
  --radius-lg: 10px;
  --radius-full: 999px;

  /* shadows (overlay-only) */
  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.04);
  --shadow-md: 0 4px 12px rgba(0, 0, 0, 0.06);
  --shadow-lg: 0 16px 48px rgba(0, 0, 0, 0.12);

  /* motion */
  --duration-base: 150ms;
  --ease-out: cubic-bezier(0, 0, 0.2, 1);
}

.dark {
  --color-background: #09090b;
  --color-foreground: #fafafa;
  --color-border: #27272a;
  --color-muted-foreground: #a1a1aa;

  --color-surface: #18181b;
  --color-surface-muted: #09090b;
  --color-surface-subtle: #27272a;

  --color-brand: #3b82f6;
  --color-brand-soft: rgba(59, 130, 246, 0.12);
  --color-success: #10b981;
  --color-success-soft: rgba(16, 185, 129, 0.14);
  --color-warning: #f59e0b;
  --color-warning-soft: rgba(245, 158, 11, 0.14);
  --color-destructive: #ef4444;
  --color-destructive-soft: rgba(239, 68, 68, 0.14);
  --color-info: #38bdf8;
  --color-info-soft: rgba(56, 189, 248, 0.14);

  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.3);
  --shadow-md: 0 4px 12px rgba(0, 0, 0, 0.4);
  --shadow-lg: 0 16px 48px rgba(0, 0, 0, 0.5);
}

@theme inline {
  --color-background: var(--color-background);
  --color-foreground: var(--color-foreground);
  --color-border: var(--color-border);
  --color-muted-foreground: var(--color-muted-foreground);

  --color-surface: var(--color-surface);
  --color-surface-muted: var(--color-surface-muted);
  --color-surface-subtle: var(--color-surface-subtle);

  --color-brand: var(--color-brand);
  --color-brand-soft: var(--color-brand-soft);
  --color-success: var(--color-success);
  --color-success-soft: var(--color-success-soft);
  --color-warning: var(--color-warning);
  --color-warning-soft: var(--color-warning-soft);
  --color-destructive: var(--color-destructive);
  --color-destructive-soft: var(--color-destructive-soft);
  --color-info: var(--color-info);
  --color-info-soft: var(--color-info-soft);

  --radius-sm: var(--radius-sm);
  --radius-md: var(--radius-md);
  --radius-lg: var(--radius-lg);
  --radius-full: var(--radius-full);

  --shadow-sm: var(--shadow-sm);
  --shadow-md: var(--shadow-md);
  --shadow-lg: var(--shadow-lg);

  --font-sans: var(--font-sans);
  --font-mono: var(--font-mono);
}

* {
  box-sizing: border-box;
}

html {
  background-color: var(--color-background);
  color-scheme: light dark;
}

body {
  margin: 0;
  min-height: 100vh;
  background-color: var(--color-background);
  color: var(--color-foreground);
  font-family: var(--font-sans);
}

a {
  color: inherit;
  text-decoration: none;
}

a[href],
button:not(:disabled),
summary {
  cursor: pointer;
}

::selection {
  background-color: var(--color-brand-soft);
  color: var(--color-foreground);
}

@keyframes skeleton-pulse {
  0%,
  100% { opacity: 1; }
  50%  { opacity: 0.42; }
}

@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    scroll-behavior: auto !important;
    transition-duration: 0.01ms !important;
  }
}
```

> **Tailwind v4 深色变体注意**：Tailwind v4 默认不内置 `.dark` 选择器。使用 `@custom-variant dark (&:where(.dark, .dark *));` 声明自定义变体；后续组件里可以 `dark:bg-surface` 正常使用。若 Tailwind v4 该项目版本语法略有差异，参考当前项目 `@tailwindcss/postcss` 版本文档调整。

## Step 4 · run 测试确认 PASS

```bash
corepack pnpm build 2>&1 | tee /tmp/t1.1-build.log
grep -E "(--surface-nav|\.interactive-card|\.reveal-list|@keyframes fade-in-up|@keyframes radar-draw|\.sidebar-tooltip)" src/app/globals.css && echo "FAIL" || echo "CLEAN"
grep -c "@fontsource/ibm-plex" src/app/globals.css  # expect 0
grep -c "\.dark" src/app/globals.css                # expect ≥ 1
grep -c "@custom-variant dark" src/app/globals.css  # expect 1
```

Expected：
- build PASS
- CLEAN（无废弃 token）
- IBM Plex 计数 0
- `.dark` 计数 ≥ 1
- `@custom-variant dark` 计数 1

## Step 5 · commit

执行阶段由 executing-phase 负责；本 planning task 不 commit。完成后的 commit 消息示例：

```bash
git add src/app/globals.css
git commit -m "feat(ui): rewrite globals.css tokens for Linear-派 design system (M1/T1.1)

- Define 6 functional colors + soft variants, zinc 9-level neutrals with semantic aliases
- Define 4-step radius (4/6/10/999px), 4-step shadows (none/sm/md/lg)
- Define 3-level surfaces (surface/muted/subtle)
- Add .dark variant for all colors, shadows, surfaces; color-scheme: light dark
- Drop body radial gradient, .interactive-card, .reveal-list, fade-in-up/radar-draw keyframes
- Drop --surface-nav indigo, IBM Plex font imports, selection indigo highlight
- Keep skeleton-pulse, prefers-reduced-motion guard"
```

<!-- handoff: H1 · status: complete · timestamp: 2026-04-15 17:05 -->
