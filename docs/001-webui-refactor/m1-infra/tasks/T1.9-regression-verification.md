---
category: testing
files_owned:
  - docs/001-webui-refactor/m1-infra/tests/T1.9-regression-report.md
files_readonly:
  - docs/001-webui-refactor/design.md
  - docs/001-webui-refactor/roadmap.md
  - src/**/*
files_shared: []
---

# T1.9 · 跨组件回归验证

## 目标

完成 Phase 1 收口前的所有回归验证，覆盖 `design.md` §11.1 和 `roadmap.md` §4.4 **Phase 1 完成判定** 的 7 条 checkbox。不改任何源代码，仅运行工具、手测、收集证据、记录结果到 `docs/001-webui-refactor/m1-infra/tests/T1.9-regression-report.md`。

## 涉及文件

- **Create**: `docs/001-webui-refactor/m1-infra/tests/T1.9-regression-report.md`
- **Readonly**: `src/**/*`（不改源代码）

## 验收条件

- [ ] `corepack pnpm db:init` 通过
- [ ] `corepack pnpm typecheck` 0 errors
- [ ] `corepack pnpm lint` 0 errors（warning 允许，但在 report 中列出）
- [ ] `corepack pnpm build` 成功，无 CSS 编译错误
- [ ] `corepack pnpm test` 全部 Vitest PASS（至少 T1.2 / T1.3 / T1.4 / T1.5 / T1.6 / T1.7 / T1.8 新增的测试全通过 + 既有测试无 regression）
- [ ] 视觉 diff 自查 7 个 feature 页面：`/practice` / `/qa` / `/questions` / `/review` / `/interviews` / `/import` / `/resume`；每页截图（浅色 + 深色两张），放到 report 里；无"崩坏"（至少能渲染，按钮 / 表单 / Badge 无错位）
- [ ] 按钮几何对齐自查：在 PageHeader 右上角和 Dialog footer 各截一排按钮（primary + secondary + ghost 组合），确认像素高度差 ≤ 0
- [ ] Tokens 双态验证：浅色 + 深色两套下所有 color / font / radius / shadow / surface tokens 正确映射（在 devtools 查 computed style）
- [ ] 主题切换器三态验证：
  - 默认 system 跟随系统（改 OS 主题，页面实时切）
  - 点按钮切 light 后刷新仍 light
  - 点按钮切 dark 后刷新仍 dark
  - `localStorage.openInterviewTheme` 值正确
- [ ] 375px viewport 自查：sidebar 塌成顶部抽屉，主区域内容不溢出
- [ ] 废弃资源 git diff 删除验证：
  ```bash
  grep -rn "surface-nav\|interactive-card\|reveal-list\|fade-in-up\|radar-draw\|sidebar-tooltip" src/app/globals.css || echo "CLEAN"
  grep -rn "ibm-plex" package.json src/ || echo "CLEAN"
  ```
  两行都输出 CLEAN
- [ ] Button / SurfaceCard / Badge 调用点 typecheck 清零：`grep -rn "Button\|SurfaceCard\|Badge\|Skeleton" src/features` 命中行全部能 typecheck 通过（最终 `corepack pnpm typecheck` 0 errors 即隐含覆盖）
- [ ] 组件 props 不兼容清单在 `T1.9-regression-report.md` 中列出（T1.11 交付文档会再用一次）

## 依赖

- 前置：T1.1 – T1.8 全部完成

## 估算

0.5 天（约 4 小时）

## 要动的文件

- Create: `docs/001-webui-refactor/m1-infra/tests/T1.9-regression-report.md`（一份 markdown，收齐所有验证证据）

## Step 1 · 写失败测试

本 task 为验收类，"失败测试"等价于**在组件实现前 run 验证命令集**——预期在开发阶段（T1.1–T1.8 未 commit 时）命令会失败。到达本 task 时，所有依赖 task 已 commit，理论上全 PASS。若不 PASS，回退到 T1.10 buffer 修复。

## Step 2 · run 测试确认 FAIL（历史锚点）

在 T1.1–T1.8 开发过程中，以下命令在每个 task 的 Step 2 分别 FAIL。到本 task 时不再需要 FAIL 佐证，直接进入 Step 3 / Step 4 的 PASS 流程。

## Step 3 · 写最小实现

本 task 不写代码，只写验收 report。创建 `docs/001-webui-refactor/m1-infra/tests/T1.9-regression-report.md`，按以下模板填充：

```markdown
# T1.9 回归验证 Report

- 验收时间：YYYY-MM-DD
- 验收人：<implementer agent id>
- 前置 task：T1.1 – T1.8 全部 merged into feature/001-webui-refactor

## 一、构建验证

| 命令 | 结果 | 输出摘要 |
|---|---|---|
| `corepack pnpm db:init` | PASS/FAIL | ... |
| `corepack pnpm typecheck` | PASS/FAIL | ... |
| `corepack pnpm lint` | PASS/FAIL | ... |
| `corepack pnpm build` | PASS/FAIL | ... |
| `corepack pnpm test` | PASS/FAIL | ... |

## 二、废弃资源清理验证

```bash
$ grep -rn "surface-nav\|interactive-card\|reveal-list\|fade-in-up\|radar-draw\|sidebar-tooltip" src/app/globals.css
<expect empty>
$ grep -rn "ibm-plex" package.json src/
<expect empty>
```

验证：CLEAN / FAIL（列出残留）

## 三、7 个 feature 页面视觉 diff 自查

| 页面 | 浅色渲染 | 深色渲染 | 崩坏项 |
|---|---|---|---|
| /practice | [screenshot] | [screenshot] | 无 / 列出 |
| /qa | ... | ... | ... |
| /questions | ... | ... | ... |
| /review | ... | ... | ... |
| /interviews | ... | ... | ... |
| /import | ... | ... | ... |
| /resume | ... | ... | ... |

已知 cosmetic 退化（M2 领地，不阻塞）：
- `.interactive-card` 已删除，影响 `resume/*.tsx` 4 处、`qa/*.tsx` 3 处的 hover 态（列出具体文件 + 行号）
- `.reveal-list` 已删除，影响 `practice/*.tsx` 2 处、`import/*.tsx` 1 处、`qa/*.tsx` 3 处的入场动画
- `@keyframes radar-draw` 已删除，影响 `practice-radar-chart.tsx` 雷达图入场

## 四、按钮几何对齐

- PageHeader 右上角按钮排：实测 primary / secondary / ghost 的 DOM bounding rect `height`（devtools `getBoundingClientRect()`）
- Dialog footer 按钮排：同上

| 位置 | Primary h | Secondary h | Ghost h | Diff |
|---|---|---|---|---|
| PageHeader | Xpx | Xpx | Xpx | 0 |
| Dialog footer | Xpx | Xpx | Xpx | 0 |

## 五、Tokens 双态映射

抽样验证（devtools Computed tab）：
- `:root` 浅色：`--color-brand: rgb(37, 99, 235)`
- `.dark` 深色：`--color-brand: rgb(59, 130, 246)`
- `--radius-md: 6px`
- `--shadow-md: 0 4px 12px rgba(0, 0, 0, 0.06)`
- `--font-sans` 第一项为 Inter

## 六、主题切换器三态

| 操作 | localStorage 值 | html class 是否含 .dark |
|---|---|---|
| 初始加载（系统 dark） | system | 是（跟随系统） |
| 点击切到 light，刷新 | light | 否 |
| 点击切到 dark，刷新 | dark | 是 |
| 点击回到 system（OS light） | system | 否 |

## 七、375px viewport 自查

- sidebar：默认隐藏（hamburger 可点开）
- 主内容区：单列，横向无溢出
- TopBar：单行，LLM pill + ThemeToggle 不换行
- PageHeader：title + actions 自适应换行
- 7 个页面逐一进入，按此清单复核

## 八、组件 props 不兼容清单（同步到 T1.11）

- `Button`：新增 `size` prop（可选，默认 md）。现有调用点未传 `size` 的会从旧 h-11=44px 收缩到 h-[34px]=34px，**这是有意的密度收敛**，非 break。
- `Badge`：新增 `variant` prop（可选，默认 soft）；tone `accent` 保留为 `brand` 的别名；去掉了强制 `uppercase`。若某处调用依赖 uppercase 视觉，需在 M2 显式加 `className="uppercase"`。
- `SurfaceCard`：`interactive` prop 保留但降级为 no-op（`.interactive-card` 全局类已删）。依赖 hover 动效的页面在 M2 重构时显式加 hover class。
- `PageHeader`：`eyebrow` prop 保留但忽略（design.md §5.3 去掉 eyebrow）；`highlights` 改走内部 StatsRow 自动渲染，行为向下兼容。

## 九、结论

- 验收：通过 / 带已知 M2 cosmetic 退化通过 / 不通过（列出阻塞问题）
- 交由 T1.10 buffer：<空 or 列出问题>
- 交由 T1.11 delivery：<不兼容清单 + 变更说明>
```

## Step 4 · run 测试确认 PASS

在 CI / 本地按 report 第一至第七节命令逐条跑。期望全 PASS 或"带已知 M2 cosmetic 退化通过"。任何 block 项不视为 PASS，必须回填 T1.10。

## Step 5 · commit

```bash
git add docs/001-webui-refactor/m1-infra/tests/T1.9-regression-report.md
git commit -m "test: M1 Phase 1 regression verification report (M1/T1.9)

- Run build/typecheck/lint/build/test chain
- Verify deprecated CSS removal (6 tokens + IBM Plex deps)
- Visual diff sweep on 7 feature pages (light + dark)
- Button geometry alignment (DOM boundingRect)
- Tokens dual-mode mapping (devtools)
- Theme toggle tri-state + localStorage memory
- 375px mobile viewport drawer
- Props incompat inventory for T1.11 delivery doc"
```

<!-- handoff: H1 · status: complete · timestamp: 2026-04-15 17:05 -->
