---
category: development
files_owned:
  - src/components/ui/theme-toggle.tsx
  - src/components/ui/theme-provider.tsx
files_readonly:
  - docs/001-webui-refactor/design.md
  - src/app/globals.css
files_shared:
  - src/app/layout.tsx
---

# T1.3 · 深色模式基础设施（theme-toggle 组件 + 三态切换 + localStorage 记忆）

## 目标

实现深色模式完整基础设施：
1. `ThemeProvider` client 组件：从 `localStorage.openInterviewTheme` 读取用户偏好 + `window.matchMedia("(prefers-color-scheme: dark)")` 跟随系统；维护 `system / light / dark` 三态；挂载 `.dark` class 到 `<html>` 根元素。
2. `theme-toggle.tsx`：三态切换按钮（icon 用 lucide 的 `Sun / Moon / Monitor`），hover 出 Tooltip（T1.6 产出），点击循环切换 system → light → dark → system。
3. `src/app/layout.tsx`：在 `<html>` 加 `suppressHydrationWarning`，在 `<body>` 内包 `ThemeProvider`；加 inline `<script>` 在 React hydrate 前同步挂 `.dark` class（避免首屏闪白）。

## 涉及文件

- **Create**: `src/components/ui/theme-provider.tsx`
- **Create**: `src/components/ui/theme-toggle.tsx`
- **Modify（shared）**: `src/app/layout.tsx`（加 `suppressHydrationWarning` + inline script + ThemeProvider 包裹）
- **Test**: `tests/theme-toggle.test.tsx`（可选 render 冒烟）

## 验收条件

- [ ] `theme-provider.tsx` 导出 `ThemeProvider` 组件 + `useTheme()` hook，返回 `{ theme: "system" | "light" | "dark", resolvedTheme: "light" | "dark", setTheme }`
- [ ] 首次加载：读 `localStorage.openInterviewTheme`，无则默认 `system`
- [ ] `system` 模式下跟随 `prefers-color-scheme`；用户切换系统主题时实时响应
- [ ] `setTheme(theme)` 更新 state + 写 `localStorage.openInterviewTheme` + 同步 `document.documentElement.classList` 的 `.dark` toggle
- [ ] `theme-toggle.tsx` 渲染 icon-only button（28px sm 档），含 `aria-label` 描述当前状态 + 下一个目标；**本 task 不依赖 Tooltip 组件**（Tooltip 在 T1.6 产出，为避免循环依赖，T1.3 的 aria-label 承载语义即可，视觉 tooltip 由 T1.6 完成后在 T1.8 中接入）
- [ ] `src/app/layout.tsx` 加 `<html lang="en" suppressHydrationWarning>`
- [ ] `src/app/layout.tsx` 在 `<head>` 或 `<body>` 前部加 inline `<script>` 在 React hydrate 前读 localStorage 并挂 `.dark` class，消除 FOUC
- [ ] `ThemeProvider` 包裹 `{children}`
- [ ] `corepack pnpm typecheck` / `lint` / `build` 全通过
- [ ] 手测：devtools 切系统主题 → 页面实时切；刷新后保留 localStorage 记忆；点按钮循环切 system → light → dark → system

## 依赖

- 前置：T1.1（globals.css 有 `.dark` 变体定义）、T1.2（字体依赖已切，layout.tsx 已改 font-sans class）
- 后置：T1.8（shell 层消费 theme-toggle 组件）必须在本 task 完成后再开始，避免 layout.tsx 并行写冲突

## 估算

0.75 天（约 6 小时）

## 要动的文件

- Create: `src/components/ui/theme-provider.tsx`
- Create: `src/components/ui/theme-toggle.tsx`
- Modify: `src/app/layout.tsx`（加 suppressHydrationWarning、inline script、ThemeProvider 包裹）
- Create（可选）: `tests/theme-toggle.test.tsx`

## Step 1 · 写失败测试

创建 `tests/theme-toggle.test.tsx`：

```typescript
// @vitest-environment jsdom
import { render, screen, fireEvent } from "@testing-library/react";
import { describe, expect, it, beforeEach } from "vitest";

import { ThemeProvider } from "@/components/ui/theme-provider";
import { ThemeToggle } from "@/components/ui/theme-toggle";

describe("ThemeToggle (M1/T1.3)", () => {
  beforeEach(() => {
    window.localStorage.clear();
    document.documentElement.classList.remove("dark");
  });

  it("renders with aria-label", () => {
    render(
      <ThemeProvider>
        <ThemeToggle />
      </ThemeProvider>,
    );
    expect(screen.getByRole("button", { name: /主题/i })).toBeInTheDocument();
  });

  it("cycles system → light → dark", () => {
    render(
      <ThemeProvider>
        <ThemeToggle />
      </ThemeProvider>,
    );
    const btn = screen.getByRole("button", { name: /主题/i });
    fireEvent.click(btn); // system → light
    expect(window.localStorage.getItem("openInterviewTheme")).toBe("light");
    fireEvent.click(btn); // light → dark
    expect(window.localStorage.getItem("openInterviewTheme")).toBe("dark");
    expect(document.documentElement.classList.contains("dark")).toBe(true);
    fireEvent.click(btn); // dark → system
    expect(window.localStorage.getItem("openInterviewTheme")).toBe("system");
  });
});
```

> 若项目尚未引入 `@testing-library/react`，该测试可降级为 pure logic 测试 ThemeProvider 的 `setTheme` 副作用（import 后直接调用，断言 document class 和 localStorage），避免新增测试依赖。

## Step 2 · run 测试确认 FAIL

```bash
npx vitest run tests/theme-toggle.test.tsx
```

Expected：FAIL with "Cannot find module '@/components/ui/theme-provider'" 或等价错误（组件未创建）。

## Step 3 · 写最小实现

### `src/components/ui/theme-provider.tsx`

```tsx
"use client";

import {
  createContext,
  useCallback,
  useContext,
  useEffect,
  useMemo,
  useState,
  type ReactNode,
} from "react";

type Theme = "system" | "light" | "dark";
type ResolvedTheme = "light" | "dark";

type ThemeContextValue = {
  theme: Theme;
  resolvedTheme: ResolvedTheme;
  setTheme: (theme: Theme) => void;
};

const STORAGE_KEY = "openInterviewTheme";

const ThemeContext = createContext<ThemeContextValue | null>(null);

function readSystemPreference(): ResolvedTheme {
  if (typeof window === "undefined") return "light";
  return window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light";
}

function readStoredTheme(): Theme {
  if (typeof window === "undefined") return "system";
  const value = window.localStorage.getItem(STORAGE_KEY);
  return value === "light" || value === "dark" || value === "system" ? value : "system";
}

function applyThemeClass(resolved: ResolvedTheme) {
  const root = document.documentElement;
  if (resolved === "dark") root.classList.add("dark");
  else root.classList.remove("dark");
}

export function ThemeProvider({ children }: { children: ReactNode }) {
  const [theme, setThemeState] = useState<Theme>("system");
  const [systemResolved, setSystemResolved] = useState<ResolvedTheme>("light");

  useEffect(() => {
    setThemeState(readStoredTheme());
    setSystemResolved(readSystemPreference());

    const mq = window.matchMedia("(prefers-color-scheme: dark)");
    const handler = (event: MediaQueryListEvent) => {
      setSystemResolved(event.matches ? "dark" : "light");
    };
    mq.addEventListener("change", handler);
    return () => mq.removeEventListener("change", handler);
  }, []);

  const resolvedTheme = useMemo<ResolvedTheme>(
    () => (theme === "system" ? systemResolved : theme),
    [theme, systemResolved],
  );

  useEffect(() => {
    applyThemeClass(resolvedTheme);
  }, [resolvedTheme]);

  const setTheme = useCallback((next: Theme) => {
    setThemeState(next);
    window.localStorage.setItem(STORAGE_KEY, next);
  }, []);

  const value = useMemo(() => ({ theme, resolvedTheme, setTheme }), [theme, resolvedTheme, setTheme]);

  return <ThemeContext.Provider value={value}>{children}</ThemeContext.Provider>;
}

export function useTheme() {
  const ctx = useContext(ThemeContext);
  if (!ctx) throw new Error("useTheme must be used within ThemeProvider");
  return ctx;
}
```

### `src/components/ui/theme-toggle.tsx`

```tsx
"use client";

import { Monitor, Moon, Sun } from "lucide-react";

import { useTheme } from "@/components/ui/theme-provider";
import { cn } from "@/lib/utils";

const LABELS = {
  system: "跟随系统主题",
  light: "浅色主题",
  dark: "深色主题",
} as const;

const NEXT = {
  system: "light",
  light: "dark",
  dark: "system",
} as const;

const ICONS = {
  system: Monitor,
  light: Sun,
  dark: Moon,
} as const;

export function ThemeToggle({ className }: { className?: string }) {
  const { theme, setTheme } = useTheme();
  const Icon = ICONS[theme];

  return (
    <button
      aria-label={`当前${LABELS[theme]}，点击切换到${LABELS[NEXT[theme]]}`}
      className={cn(
        "inline-flex h-7 w-7 items-center justify-center rounded-[var(--radius-md)] border border-[color:var(--color-border)] bg-[color:var(--color-surface)] text-[color:var(--color-foreground)] transition-colors duration-150 ease-out hover:bg-[color:var(--color-surface-subtle)] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[color:var(--color-brand)] focus-visible:ring-offset-2",
        className,
      )}
      onClick={() => setTheme(NEXT[theme])}
      type="button"
    >
      <Icon className="h-4 w-4" />
    </button>
  );
}
```

### `src/app/layout.tsx`（基于 T1.2 产物叠加修改）

```tsx
import type { Metadata } from "next";

import { ThemeProvider } from "@/components/ui/theme-provider";
import "./globals.css";

export const metadata: Metadata = {
  title: {
    default: "Open Interview",
    template: "%s | Open Interview",
  },
  description:
    "Open Interview is a local-first workbench for interview notes, question review, grounded QA, and resume deep dives.",
};

const themeBootstrap = `(function(){try{var t=localStorage.getItem('openInterviewTheme');var d=t==='dark'||(t!=='light'&&window.matchMedia('(prefers-color-scheme: dark)').matches);if(d)document.documentElement.classList.add('dark');}catch(e){}})();`;

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <head>
        <script dangerouslySetInnerHTML={{ __html: themeBootstrap }} />
      </head>
      <body className="font-sans antialiased">
        <ThemeProvider>{children}</ThemeProvider>
      </body>
    </html>
  );
}
```

## Step 4 · run 测试确认 PASS

```bash
corepack pnpm typecheck
corepack pnpm lint
corepack pnpm build
npx vitest run tests/theme-toggle.test.tsx
```

Expected：全 PASS。

## Step 5 · commit

```bash
git add src/components/ui/theme-provider.tsx src/components/ui/theme-toggle.tsx src/app/layout.tsx tests/theme-toggle.test.tsx
git commit -m "feat(ui): add ThemeProvider, ThemeToggle and localStorage-memoized dark mode (M1/T1.3)

- Add ThemeProvider client component with system/light/dark tri-state
- Sync .dark class on <html>; remember choice via localStorage.openInterviewTheme
- Add ThemeToggle icon-only button cycling system → light → dark
- Inline bootstrap script in root layout to avoid FOUC on first paint"
```

<!-- handoff: H1 · status: complete · timestamp: 2026-04-15 17:05 -->
