---
category: development
files_owned:
  - src/components/ui/card.tsx
  - src/components/ui/surface-card.tsx
  - src/components/ui/tooltip.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/separator.tsx
files_readonly:
  - docs/001-webui-refactor/design.md
  - src/app/globals.css
files_shared: []
---

# T1.6 · Card 拆分 + 新增组件（tooltip / dialog / separator）

## 目标

1. **Card 拆分**：新建 `src/components/ui/card.tsx`，导出 `Card / CardHeader / CardBody / CardFooter` 子组件；基于 surface / surface-muted tokens + `rounded-[var(--radius-lg)]`（10px）+ `border`；**不使用**阴影（阴影仅 overlay）。
2. **surface-card.tsx 保留**作为向下兼容入口：`export { Card as SurfaceCard }`，去除 `interactive` 属性对应的 `interactive-card` 全局类（该类已被 T1.1 删除）；改为**纯数据属性 `data-interactive` + hover Tailwind 类**或完全砍掉 `interactive` prop（由于 `grep` 命中仅 5 处都在 resume/* 和 qa/*.tsx M2 领地，可把 `interactive` 降级为"接受但 no-op"并在 JSDoc 标 `@deprecated`）。
3. **tooltip.tsx**：受控 / 非受控 tooltip 组件，hover / focus 触发，用 `shadow-md` + `var(--radius-md)`；纯 CSS 实现（不引入 Radix，以保持本 MVP 的零额外依赖原则）；承载 `design.md` §5.9 中"`.sidebar-tooltip` 改走规范 Tooltip"的角色。
4. **dialog.tsx**：基础 modal dialog 组件（open / onOpenChange），带 backdrop + `shadow-lg` + `rounded-[var(--radius-lg)]`；纯 React portal 实现（document.body），键盘 ESC 关闭 + 锁 body scroll。
5. **separator.tsx**：水平 / 垂直 separator，`1px bg-[color:var(--color-border)]`，替换现有散落的 `<div className="border-t" />`。

## 涉及文件

- **Create**: `src/components/ui/card.tsx`
- **Modify**: `src/components/ui/surface-card.tsx`（改为 re-export + deprecated wrapper）
- **Create**: `src/components/ui/tooltip.tsx`
- **Create**: `src/components/ui/dialog.tsx`
- **Create**: `src/components/ui/separator.tsx`
- **Test**: `tests/card-and-new-ui.test.tsx`

## 验收条件

- [ ] `Card` 组件支持 `muted` prop（`false` → `bg-surface`、`true` → `bg-surface-muted`）；padding 默认 `p-5`；radius `var(--radius-lg)`；border `border-[color:var(--color-border)]`
- [ ] `CardHeader / CardBody / CardFooter` 分别对应 header / body / footer 语义；间距遵循 design.md §5.4（Header 紧凑档 10–16px、Body 平衡档 18–20px）
- [ ] `SurfaceCard` 作为 `Card` 的别名或 thin wrapper 导出，保留 `muted / interactive / className` props；`interactive` 降级为接受但不 apply `interactive-card` 全局类（可改为 Tailwind `hover:border-brand hover:bg-brand-soft` 等效规则，但放在 M2 页面自行处理更合适；本 task 先接受 prop 但忽略或 no-op）
- [ ] `Tooltip` 支持 `content` prop 和 `children` 触发元素；hover / focus 显示；`shadow-md` + `rounded-[var(--radius-md)]` + `bg-[color:var(--color-surface)]` + `border-[color:var(--color-border)]`；深色模式自动通过 CSS 变量切换
- [ ] `Dialog` 支持 `open`、`onOpenChange`、`children`（trigger + content）；ESC 关闭；click backdrop 关闭；`body { overflow: hidden }` 锁定
- [ ] `Separator` 支持 `orientation="horizontal" | "vertical"`，默认 horizontal；`role="separator"` + `aria-orientation`
- [ ] 所有组件 `corepack pnpm typecheck` 通过
- [ ] Vitest render 冒烟 PASS

## 依赖

- 前置：T1.1（tokens 就绪）
- 后置：T1.7（EmptyList 等组件可能用 Card / CardFooter）、T1.8（sidebar / topbar 使用 Tooltip）

## 估算

1 天

## 要动的文件

- Create: `src/components/ui/card.tsx`（~60 行）
- Modify: `src/components/ui/surface-card.tsx`（28 行 → ~25 行 re-export）
- Create: `src/components/ui/tooltip.tsx`（~50 行）
- Create: `src/components/ui/dialog.tsx`（~90 行）
- Create: `src/components/ui/separator.tsx`（~25 行）
- Create: `tests/card-and-new-ui.test.tsx`

## Step 1 · 写失败测试

创建 `tests/card-and-new-ui.test.tsx`：

```typescript
// @vitest-environment jsdom
import { render, screen, fireEvent } from "@testing-library/react";
import { describe, expect, it } from "vitest";

import { Card, CardHeader, CardBody, CardFooter } from "@/components/ui/card";
import { SurfaceCard } from "@/components/ui/surface-card";
import { Tooltip } from "@/components/ui/tooltip";
import { Dialog } from "@/components/ui/dialog";
import { Separator } from "@/components/ui/separator";

describe("Card and new UI (M1/T1.6)", () => {
  it("Card renders subcomponents", () => {
    render(
      <Card>
        <CardHeader>Header</CardHeader>
        <CardBody>Body</CardBody>
        <CardFooter>Footer</CardFooter>
      </Card>,
    );
    expect(screen.getByText("Header")).toBeInTheDocument();
    expect(screen.getByText("Body")).toBeInTheDocument();
    expect(screen.getByText("Footer")).toBeInTheDocument();
  });

  it("SurfaceCard still works as alias", () => {
    render(<SurfaceCard muted>Legacy</SurfaceCard>);
    expect(screen.getByText("Legacy")).toBeInTheDocument();
  });

  it("Tooltip shows content on hover", () => {
    const { container } = render(
      <Tooltip content="Hint">
        <button>Target</button>
      </Tooltip>,
    );
    const trigger = screen.getByText("Target");
    fireEvent.mouseEnter(trigger);
    expect(container.textContent).toMatch(/Hint/);
  });

  it("Dialog opens when open=true", () => {
    render(
      <Dialog open onOpenChange={() => undefined}>
        <div>Modal content</div>
      </Dialog>,
    );
    expect(screen.getByText("Modal content")).toBeInTheDocument();
  });

  it("Separator has role=separator", () => {
    const { container } = render(<Separator />);
    expect(container.querySelector("[role='separator']")).toBeTruthy();
  });
});
```

## Step 2 · run 测试确认 FAIL

```bash
npx vitest run tests/card-and-new-ui.test.tsx
```

Expected：FAIL with `Cannot find module '@/components/ui/card'` 等。

## Step 3 · 写最小实现

### `src/components/ui/card.tsx`

```tsx
import { cn } from "@/lib/utils";

type CardProps = React.HTMLAttributes<HTMLDivElement> & {
  muted?: boolean;
};

export function Card({ className, muted = false, ...props }: CardProps) {
  return (
    <div
      className={cn(
        "rounded-[var(--radius-lg)] border border-[color:var(--color-border)]",
        muted ? "bg-[color:var(--color-surface-muted)]" : "bg-[color:var(--color-surface)]",
        className,
      )}
      {...props}
    />
  );
}

export function CardHeader({
  className,
  ...props
}: React.HTMLAttributes<HTMLDivElement>) {
  return (
    <div
      className={cn("px-5 py-3 border-b border-[color:var(--color-border)]", className)}
      {...props}
    />
  );
}

export function CardBody({
  className,
  ...props
}: React.HTMLAttributes<HTMLDivElement>) {
  return <div className={cn("p-5", className)} {...props} />;
}

export function CardFooter({
  className,
  ...props
}: React.HTMLAttributes<HTMLDivElement>) {
  return (
    <div
      className={cn(
        "flex items-center justify-end gap-2 px-5 py-3 border-t border-[color:var(--color-border)]",
        className,
      )}
      {...props}
    />
  );
}
```

### `src/components/ui/surface-card.tsx`（向下兼容）

```tsx
import { Card } from "@/components/ui/card";
import { cn } from "@/lib/utils";

type SurfaceCardProps = React.HTMLAttributes<HTMLDivElement> & {
  muted?: boolean;
  /** @deprecated interactive-card CSS was removed; pass hover classes via className if needed */
  interactive?: boolean;
};

export function SurfaceCard({
  className,
  interactive: _interactive,
  muted = false,
  ...props
}: SurfaceCardProps) {
  // `interactive` accepted for backward compat; hover effects should be provided via className
  return <Card className={cn("p-5", className)} muted={muted} {...props} />;
}
```

### `src/components/ui/tooltip.tsx`

```tsx
"use client";

import { useState, type ReactElement, type ReactNode } from "react";

import { cn } from "@/lib/utils";

type TooltipProps = {
  content: ReactNode;
  children: ReactElement;
  side?: "top" | "right" | "bottom" | "left";
  className?: string;
};

export function Tooltip({ content, children, side = "top", className }: TooltipProps) {
  const [open, setOpen] = useState(false);

  const sideClasses: Record<NonNullable<TooltipProps["side"]>, string> = {
    top: "bottom-[calc(100%+6px)] left-1/2 -translate-x-1/2",
    right: "left-[calc(100%+6px)] top-1/2 -translate-y-1/2",
    bottom: "top-[calc(100%+6px)] left-1/2 -translate-x-1/2",
    left: "right-[calc(100%+6px)] top-1/2 -translate-y-1/2",
  };

  return (
    <span
      className="relative inline-flex"
      onBlur={() => setOpen(false)}
      onFocus={() => setOpen(true)}
      onMouseEnter={() => setOpen(true)}
      onMouseLeave={() => setOpen(false)}
    >
      {children}
      {open ? (
        <span
          role="tooltip"
          className={cn(
            "pointer-events-none absolute z-30 whitespace-nowrap rounded-[var(--radius-md)] border border-[color:var(--color-border)] bg-[color:var(--color-surface)] px-2 py-1 text-[11px] font-medium text-[color:var(--color-foreground)] shadow-[var(--shadow-md)]",
            sideClasses[side],
            className,
          )}
        >
          {content}
        </span>
      ) : null}
    </span>
  );
}
```

### `src/components/ui/dialog.tsx`

```tsx
"use client";

import { useEffect, type ReactNode } from "react";
import { createPortal } from "react-dom";

import { cn } from "@/lib/utils";

type DialogProps = {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  children: ReactNode;
  className?: string;
  labelledBy?: string;
};

export function Dialog({ open, onOpenChange, children, className, labelledBy }: DialogProps) {
  useEffect(() => {
    if (!open) return;
    const onKey = (event: KeyboardEvent) => {
      if (event.key === "Escape") onOpenChange(false);
    };
    const prevOverflow = document.body.style.overflow;
    document.body.style.overflow = "hidden";
    window.addEventListener("keydown", onKey);
    return () => {
      document.body.style.overflow = prevOverflow;
      window.removeEventListener("keydown", onKey);
    };
  }, [open, onOpenChange]);

  if (!open || typeof document === "undefined") return null;

  return createPortal(
    <div
      aria-labelledby={labelledBy}
      aria-modal="true"
      className="fixed inset-0 z-50 flex items-center justify-center"
      role="dialog"
    >
      <div
        aria-hidden="true"
        className="absolute inset-0 bg-black/40"
        onClick={() => onOpenChange(false)}
      />
      <div
        className={cn(
          "relative z-10 max-h-[90vh] w-[92vw] max-w-lg overflow-auto rounded-[var(--radius-lg)] border border-[color:var(--color-border)] bg-[color:var(--color-surface)] shadow-[var(--shadow-lg)]",
          className,
        )}
      >
        {children}
      </div>
    </div>,
    document.body,
  );
}
```

### `src/components/ui/separator.tsx`

```tsx
import { cn } from "@/lib/utils";

type SeparatorProps = React.HTMLAttributes<HTMLDivElement> & {
  orientation?: "horizontal" | "vertical";
};

export function Separator({
  className,
  orientation = "horizontal",
  ...props
}: SeparatorProps) {
  return (
    <div
      role="separator"
      aria-orientation={orientation}
      className={cn(
        "bg-[color:var(--color-border)]",
        orientation === "horizontal" ? "h-px w-full" : "h-full w-px",
        className,
      )}
      {...props}
    />
  );
}
```

## Step 4 · run 测试确认 PASS

```bash
npx vitest run tests/card-and-new-ui.test.tsx
corepack pnpm typecheck
corepack pnpm lint
```

Expected：Vitest 5 断言 PASS；typecheck 通过（SurfaceCard 保留 `muted / interactive` 签名，现有下游 5 处 M2 调用点不 break）；lint 通过。

## Step 5 · commit

```bash
git add src/components/ui/card.tsx src/components/ui/surface-card.tsx src/components/ui/tooltip.tsx src/components/ui/dialog.tsx src/components/ui/separator.tsx tests/card-and-new-ui.test.tsx
git commit -m "feat(ui): add Card/Tooltip/Dialog/Separator, keep SurfaceCard as deprecated alias (M1/T1.6)

- Card + CardHeader/CardBody/CardFooter with radius-lg + border-only elevation
- SurfaceCard becomes thin alias over Card; interactive prop deprecated (no-op)
- Tooltip: CSS-only impl with shadow-md + radius-md, no Radix dep
- Dialog: portal-based modal with backdrop + ESC close + body scroll lock, shadow-lg + radius-lg
- Separator: 1px border-colored, horizontal/vertical with aria-orientation"
```

<!-- handoff: H1 · status: complete · timestamp: 2026-04-15 17:05 -->
