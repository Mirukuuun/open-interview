# T1.9 回归验证 Report

- 验收时间：2026-04-15
- 验收人：implementer SubAgent (H10-T1.9)
- 前置 task：T1.1 – T1.8 全部已 merged into `feature/001-webui-refactor`
- 验收范围：`design.md` §11.1 / `roadmap.md` §4.4 / `m1-infra/plan.md` §验收标准 的 7 条 checkbox
- 工具链环境：Node 22+，pnpm@10.32.1，Tailwind v4.2，Next 16

## 一、构建验证

所有命令均在本轮 session 中全量执行，下表为现场输出摘要（命令均由 `corepack pnpm` 驱动）。

| 命令 | 结果 | 输出摘要 |
|---|---|---|
| `corepack pnpm db:init` | PASS | `SQLite database ready at /root/.openclaw/projects/open-interview/storage/open-interview.sqlite` |
| `corepack pnpm typecheck` | PASS | `tsc --noEmit` 无任何输出（0 errors） |
| `corepack pnpm lint` | PASS | `eslint .` 无任何输出（0 error / 0 warning） |
| `corepack pnpm build` | PASS | `✓ Compiled successfully in 12.8s` / `✓ Generating static pages using 3 workers (14/14) in 216ms` / 41 条路由全部生成；**1 条非阻塞 NFT warning**（见下） |
| `corepack pnpm test` | PASS | `Test Files 18 passed (18)` / `Tests 83 passed (83)` / Duration ~9.5s；含 T1.2 ~ T1.8 新增的 6 个测试文件：`fonts-swap.test.ts`、`button.test.tsx`、`atomic-ui.test.tsx`、`card-and-new-ui.test.tsx`、`theme-toggle.test.tsx`、`workbench-atoms.test.tsx`、`shell-smoke.test.tsx` |

**Build NFT Warning（非阻塞）**：

```
Encountered unexpected file in NFT list
A file was traced that indicates that the whole project was traced unintentionally.
Import trace:
  App Route:
    ./next.config.ts
    ./src/server/db/paths.ts
    ./src/server/db/client.ts
    ./src/server/services/interview-question-service.ts
    ./src/app/api/interview-questions/[interviewQuestionId]/promote/route.ts
```

- 性质：Next.js 文件追踪器的"动态 require"建议提示，不是编译错误
- 状态：compile 依旧 `✓ Compiled successfully`，14/14 页面生成，41 条路由全部编排成功
- 来源：`src/server/db/paths.ts` 的 `process.cwd()` 动态拼接；属于既有服务端 DB 逻辑，与本次 UI 重构无关
- 裁决：**非本次回归范围**，不阻塞 Phase 1 验收；如需消除，归入独立的 server-side 技术债

**测试覆盖汇总（18 套）**：

```
 ✓ tests/parse-job-async.test.ts (3 tests)
 ✓ tests/practice-service.test.ts (3 tests)
 ✓ tests/build-artifact.test.ts (4 tests)
 ✓ tests/interview-question-decoupling.test.ts (5 tests)
 ✓ tests/parse-source.test.ts (5 tests)
 ✓ tests/llm-provider-health.test.ts (4 tests)
 ✓ tests/single-answer-question-bank.test.ts (2 tests)
 ✓ tests/openclaw-provider-config.test.ts (4 tests)
 ✓ tests/qa-rag.test.ts (10 tests)
 ✓ tests/taxonomy-display.test.ts (4 tests)
 ✓ tests/prompt-markdown.test.ts (3 tests)
 ✓ tests/fonts-swap.test.ts (4 tests)         ← T1.2 新增
 ✓ tests/card-and-new-ui.test.tsx (5 tests)    ← T1.6 新增
 ✓ tests/shell-smoke.test.tsx (5 tests)        ← T1.8 新增
 ✓ tests/theme-toggle.test.tsx (2 tests)       ← T1.3 新增
 ✓ tests/workbench-atoms.test.tsx (8 tests)    ← T1.7 新增
 ✓ tests/atomic-ui.test.tsx (6 tests)          ← T1.5 新增
 ✓ tests/button.test.tsx (6 tests)             ← T1.4 新增

 Test Files  18 passed (18)
      Tests  83 passed (83)
```

## 二、废弃资源清理验证

### 2.1 globals.css 废弃 tokens

```bash
$ grep -rn "surface-nav\|interactive-card\|reveal-list\|fade-in-up\|radar-draw\|sidebar-tooltip" src/app/globals.css
# empty
```

**验证**：CLEAN（6 个废弃 token 在 `src/app/globals.css` 中已全部删除）

### 2.2 IBM Plex 依赖

```bash
$ grep -rn "ibm-plex" package.json src/
# empty
$ grep -rn -i "ibm-plex\|ibm plex\|IBM Plex" package.json src/
# empty
```

**验证**：CLEAN
- `package.json` `dependencies` 仅含 `@fontsource/inter ^5.2.8` + `@fontsource/jetbrains-mono ^5.2.8`（无 `@fontsource/ibm-plex-sans` / `@fontsource/ibm-plex-mono`）
- `src/` 下所有 `.ts` / `.tsx` / `.css` 文件无 IBM Plex 字面量引用
- `tests/fonts-swap.test.ts` 的 4 个断言全部通过，确保 "removes IBM Plex Sans" / "removes IBM Plex Mono" 持续生效
- 仅 `docs/archive/*.md` 和任务 spec 历史行文有引用（文档档案，不属于源代码）

### 2.3 其他废弃 CSS 产物（追加核查）

| 目标 | 扫描路径 | 命中 | 结论 |
|---|---|---|---|
| `--surface-nav` 变量 | `src/**/*` | 0 | CLEAN |
| `@keyframes fade-in-up` / `radar-draw` / `reveal*` 定义 | `src/**/*` | 0 | CLEAN（全局 keyframes 已删） |
| `animation: radar-draw` / `fade-in-up` 使用 | `src/**/*` | 1（`src/features/practice/practice-radar-chart.tsx:156`）| M2 领地：见第三节 cosmetic 退化清单 |
| `.sidebar-tooltip` 类 | `src/**/*` | 0 | CLEAN |
| `body` radial gradient (global) | `src/app/globals.css` | 0 | CLEAN（`body` 选择器仅设 margin / min-height / bg / color / font） |
| 魔术高度 `calc(100vh - 77px)` | `src/**/*` | 0 | CLEAN（T1.8 shell 重构已去除） |

### 2.4 globals.css 新 tokens 存在性（Step 3.5 补充验证）

在 `src/app/globals.css` 浅色 `:root` + `.dark` 双态块逐一核对：

| Token 分类 | 浅色 `:root` | 深色 `.dark` | `@theme inline` 映射 |
|---|---|---|---|
| typography (`--font-sans` / `--font-mono`) | ✓ | 继承 | ✓ |
| neutrals (`--color-background` / `--color-foreground` / `--color-border` / `--color-muted-foreground`) | ✓ | ✓ | ✓ |
| surfaces (`--color-surface` / `--color-surface-muted` / `--color-surface-subtle`) | ✓ | ✓ | ✓ |
| functional colors × 5（brand / success / warning / destructive / info 各含 + soft 变体） | ✓（10 条） | ✓（10 条） | ✓ |
| radii (`--radius-sm/md/lg/full`) | ✓ | 继承 | ✓ |
| shadows (`--shadow-sm/md/lg`) | ✓ | ✓（dark 加深不透明度） | ✓ |
| motion (`--duration-base` / `--ease-out`) | ✓ | 继承 | — |

全部 30+ tokens 双态完整，无遗漏。

## 三、7 个 feature 页面视觉 diff 自查

**SubAgent 无浏览器，不能产出 screenshot，本节整体标注 `[ ] 待用户手动验收`。**

用户手动验收清单（按顺序执行）：

1. 启动本地开发服务：`corepack pnpm dev`（监听 http://localhost:3000）
2. 打开两个并排浏览器窗口，分别置 light（`localStorage.openInterviewTheme='light'` 后刷新）和 dark（设为 `'dark'`）
3. 依次访问下列 7 个 URL，每个页面对比浅色 / 深色两态，记录崩坏项

| 页面 | URL | 浅色渲染 | 深色渲染 | 已知 cosmetic 退化（M2 领地） | 崩坏项（由用户填） |
|---|---|---|---|---|---|
| `/practice` | http://localhost:3000/practice | [ ] 待验收 | [ ] 待验收 | `reveal-list` 入场动画缺失（practice-recent-exams-panel.tsx:31）；雷达图 radar-draw 动画缺失（practice-radar-chart.tsx:156）；`border-border-strong` / `text-text-muted` 等旧类无定义 | — |
| `/qa` | http://localhost:3000/qa | [ ] 待验收 | [ ] 待验收 | `interactive-card` hover 失效（qa-workbench.tsx:108 / qa-ask-form.tsx:197 / qa-workbench-shell.tsx:119,324 / qa-session-workbench.tsx:214）；`reveal-list` 入场动画缺失（qa-workbench.tsx:105 / qa-workbench-shell.tsx:317,422）；`eyebrow` prop 已被忽略（qa-workbench.tsx:57 仍传但不渲染） | — |
| `/questions` | http://localhost:3000/questions | [ ] 待验收 | [ ] 待验收 | `border-border-strong` / `text-text-muted` 等旧类无 CSS 变量（question-bank-workbench.tsx / question-detail-workbench.tsx） | — |
| `/review` | http://localhost:3000/review | [ ] 待验收 | [ ] 待验收 | 同 `/questions`；Dialog footer 按钮排列（review-confirm-dialog.tsx）用于第四节几何测量 | — |
| `/interviews` | http://localhost:3000/interviews | [ ] 待验收 | [ ] 待验收 | `border-border-strong` / `text-text-muted` 等旧类无定义（interview-list-workbench.tsx / interview-detail-workbench.tsx / interview-question-card.tsx） | — |
| `/import` | http://localhost:3000/import | [ ] 待验收 | [ ] 待验收 | `reveal-list` 入场动画缺失（import-workbench.tsx:143）；`border-border-strong` 等旧类无定义 | — |
| `/resume` | http://localhost:3000/resume | [ ] 待验收 | [ ] 待验收 | `interactive-card` hover 失效（resume-workbench.tsx:125,162 / resume-detail-workbench.tsx:56 / project-workbench.tsx:118 / project-session-workbench.tsx:158，共 5 处）；`border-border-strong` 等旧类无定义 | — |

**判定规则**：
- "无崩坏" = 页面能渲染、按钮 / 表单 / Badge 无错位、主按钮不出现白底白字等；**hover 动效缺失不算崩坏**（M2 领地已登记）。
- 任何布局塌陷 / 文字不可读 / 按钮叠在一起 / Badge 变空矩形等，视为 block 项，需回填 T1.10 buffer。

### 3.1 已登记的 M2 cosmetic 退化清单（来自 `globals.css` 旧类废除的副作用）

#### (a) `.interactive-card` hover 类（全局 CSS 类已删；`SurfaceCard.interactive` 降级为 no-op）

| 文件 | 行号 | 现状 className |
|---|---|---|
| `src/features/qa/qa-session-workbench.tsx` | 214 | `interactive-card block rounded-xl border border-border-strong ...` |
| `src/features/qa/qa-workbench.tsx` | 108 | `interactive-card block rounded-[24px] border border-border-strong ...` |
| `src/features/qa/qa-ask-form.tsx` | 197 | `interactive-card rounded-full border border-border-muted ...` |
| `src/features/qa/qa-workbench-shell.tsx` | 119 | `interactive-card block rounded-2xl border border-border-muted ...` |
| `src/features/qa/qa-workbench-shell.tsx` | 324 | `interactive-card rounded-2xl border px-3 py-3` |
| `src/features/resume/project-workbench.tsx` | 118 | `interactive-card block rounded-xl border border-border-strong ...` |
| `src/features/resume/resume-detail-workbench.tsx` | 56 | `interactive-card block rounded-xl border border-border-strong ...` |
| `src/features/resume/resume-workbench.tsx` | 125, 162 | `interactive-card block rounded-xl border border-border-strong ...` |
| `src/features/resume/project-session-workbench.tsx` | 158 | `interactive-card block rounded-xl border border-border-muted ...` |

合计 **10 处**（qa 5 处、resume 5 处）。影响：hover 态的 box-shadow / border 色过渡不再生效；静态表现不受影响。

#### (b) `.reveal-list` 入场动画（全局 `@keyframes` 已删）

| 文件 | 行号 |
|---|---|
| `src/features/qa/qa-workbench.tsx` | 105 |
| `src/features/qa/qa-workbench-shell.tsx` | 317, 422 |
| `src/features/import/import-workbench.tsx` | 143 |
| `src/features/practice/practice-recent-exams-panel.tsx` | 31 |

合计 **5 处**。影响：列表入场的 fade-in-up 动画缺失；内容仍正常显示。

#### (c) `@keyframes radar-draw`（全局 keyframes 已删）

| 文件 | 行号 | 引用形式 |
|---|---|---|
| `src/features/practice/practice-radar-chart.tsx` | 156 | `style={{ animation: "radar-draw 900ms ease-out forwards" }}` |

**1 处**。影响：`/practice` 雷达图不再"画出来"，直接静态显示；功能不受影响。

#### (d) 旧 token 类名（T1.1 已替换为 `--color-*` 变量体系，旧 Tailwind 工具类无定义）

| 旧类名 | features/ 命中次数 | 可能影响 |
|---|---|---|
| `border-border-strong` / `border-border-muted` / `text-text-strong` / `text-text-muted` / `bg-accent` / `text-accent` 合集 | **430 处 / 37 文件**（grep count 汇总） | 对应的 CSS 变量（`--color-border-strong` 等）在新 `globals.css` 中不存在；Tailwind v4 会编译成 `border-color: var(--color-border-strong)` 但变量解析为空，导致 border / 文字 color 回落到默认继承 |

裁决：与 interactive-card / reveal-list 属同一类"旧视觉遗产，M2 逐页替换"。Phase 1 不阻塞，但需在 T1.11 delivery doc 中显式登记，避免 M2 实施时遗漏。

#### (e) 内联 radial-gradient（feature 级，非 global `body` gradient）

| 文件 | 行号 | 说明 |
|---|---|---|
| `src/features/qa/qa-workbench-shell.tsx` | 397 | inline `bg-[radial-gradient(...)]`；**不是** spec 里禁止的 `body` radial gradient（globals.css 已 clean），而是 QA shell 的局部装饰背景 |

**裁决**：不属于 Phase 1 违规，但 QA 页面 M2 重构时若遵循"克制优先"应考虑去掉。

## 四、按钮几何对齐

**SubAgent 无浏览器 / 无 DOM 运行时，`getBoundingClientRect()` 实测由用户手动完成。本节标注 `[ ] 待用户手动验收`。**

### 4.1 源码级几何约束（静态审阅）

`src/components/ui/button.tsx` size 矩阵：

```ts
const sizeClasses: Record<ButtonSize, string> = {
  sm: "h-7 px-2.5 text-xs rounded-[var(--radius-md)]",       // 28px
  md: "h-[34px] px-3.5 text-[13px] rounded-[var(--radius-md)]", // 34px（默认）
  lg: "h-10 px-4.5 text-sm rounded-[var(--radius-md)]",       // 40px
  xl: "h-[46px] px-6.5 text-[15px] rounded-[var(--radius-md)]", // 46px
};
```

所有 5 个 variant（primary / secondary / ghost / destructive / link）共用 `base` 样式 + `1px border`（`link` 例外无 border/height）。**理论几何严格对齐**：同 size 下跨 variant height 固定一致（`h-7 / h-[34px] / h-10 / h-[46px]`），padding 与 border 同源。

### 4.2 用户手动验收步骤

1. 启动 `corepack pnpm dev`
2. 打开 http://localhost:3000/qa（PageHeader 右上角有 `primary` + `secondary` 组合），打开 devtools → Console
3. 执行：
   ```js
   const btns = document.querySelectorAll('header button, header a[class*="inline-flex"]');
   [...btns].forEach(b => console.log(b.textContent.trim(), b.getBoundingClientRect().height));
   ```
   期望：所有 `height` 相等（默认 md = 34px），diff = 0
4. 打开 `/review` 触发 Dialog（点"复核并确认"或任一弹窗），在 footer 执行同样脚本
   期望：Dialog footer 的 primary / secondary / ghost 三种按钮 height 全部相等

| 位置 | Primary h | Secondary h | Ghost h | Destructive h | Diff | 验收 |
|---|---|---|---|---|---|---|
| `/qa` PageHeader | 待填 px | 待填 px | — | — | 待填 | [ ] 待用户手动验收 |
| Dialog footer（`review-confirm-dialog`） | 待填 px | 待填 px | 待填 px | 待填 px | 待填 | [ ] 待用户手动验收 |

## 五、Tokens 双态映射

**SubAgent 无 devtools Computed 抽样能力。本节源码验证 PASS + 用户手动 devtools 核对 `[ ] 待用户手动验收`。**

### 5.1 源码验证（静态抽样）

| Token | 浅色 `:root` 期望 | 深色 `.dark` 期望 | 实际值（`src/app/globals.css`） |
|---|---|---|---|
| `--color-brand` | #2563eb | #3b82f6 | 浅色 `#2563eb` ✓ / 深色 `#3b82f6` ✓（line 25, 62） |
| `--color-background` | #fafafa | #09090b | 浅色 `#fafafa` ✓ / 深色 `#09090b` ✓（line 14, 53） |
| `--color-foreground` | #09090b | #fafafa | 浅色 `#09090b` ✓ / 深色 `#fafafa` ✓（line 15, 54） |
| `--color-border` | #e4e4e7 | #27272a | ✓（line 16, 55） |
| `--color-surface` | #ffffff | #18181b | ✓（line 20, 58） |
| `--color-surface-subtle` | #f4f4f5 | #27272a | ✓（line 22, 60） |
| `--radius-sm` | 4px | (inherit) | ✓（line 37） |
| `--radius-md` | 6px | (inherit) | ✓（line 38） |
| `--radius-lg` | 10px | (inherit) | ✓（line 39） |
| `--radius-full` | 999px | (inherit) | ✓（line 40） |
| `--shadow-sm` | `0 1px 2px rgba(0,0,0,0.04)` | `0 1px 2px rgba(0,0,0,0.3)` | ✓（line 43, 73） |
| `--shadow-md` | `0 4px 12px rgba(0,0,0,0.06)` | `0 4px 12px rgba(0,0,0,0.4)` | ✓（line 44, 74） |
| `--shadow-lg` | `0 16px 48px rgba(0,0,0,0.12)` | `0 16px 48px rgba(0,0,0,0.5)` | ✓（line 45, 75） |
| `--font-sans` 首项 | `"Inter"` | (inherit) | ✓（line 10） |
| `--font-mono` 首项 | `"JetBrains Mono"` | (inherit) | ✓（line 11） |

源码抽样：**全部通过**。Tailwind v4 `@theme inline` 块（line 78-110）把 30+ tokens 全部转发到 Tailwind 工具类。

### 5.2 用户 devtools 手动核对清单

1. 启动 `corepack pnpm dev` → http://localhost:3000/practice
2. 浅色模式下 devtools → Elements → 选中 `<html>` → Computed tab，筛 `--color-` 抽样：
   - [ ] `--color-brand: rgb(37, 99, 235)`（等价 `#2563eb`）
   - [ ] `--color-background: rgb(250, 250, 250)`（`#fafafa`）
   - [ ] `--color-foreground: rgb(9, 9, 11)`（`#09090b`）
   - [ ] `--radius-md: 6px`
   - [ ] `--shadow-md: 0px 4px 12px rgba(0, 0, 0, 0.06)`
   - [ ] `font-family` 中 Inter 在第一位
3. Console 执行 `document.documentElement.classList.add('dark')` 切换到深色，重抽：
   - [ ] `--color-brand: rgb(59, 130, 246)`（`#3b82f6`）
   - [ ] `--color-background: rgb(9, 9, 11)`（`#09090b`）
   - [ ] `--color-foreground: rgb(250, 250, 250)`（`#fafafa`）
   - [ ] `--shadow-md: 0px 4px 12px rgba(0, 0, 0, 0.4)`

## 六、主题切换器三态

**SubAgent 不能与浏览器交互（点击 + 刷新页面）。本节源码验证 PASS + 用户手动 `[ ] 待用户手动验收`。**

### 6.1 源码验证

- `src/components/ui/theme-provider.tsx`：
  - `STORAGE_KEY = "openInterviewTheme"` ✓ 与 spec 一致
  - `Theme = "system" | "light" | "dark"` ✓ 三态完整
  - `setTheme` 同时 setState 和 `localStorage.setItem` ✓ 记忆正确
  - 监听 `matchMedia("(prefers-color-scheme: dark)")` 变化事件 → setSystemResolved → useEffect 应用 class ✓ system 跟随系统
- `src/app/layout.tsx`：提前在 `<head>` 注入 `themeBootstrap` IIFE（line 15），读 `localStorage.openInterviewTheme`，早于 React hydrate 给 `<html>` 打上 `.dark` class，避免闪烁 ✓
- `src/components/ui/theme-toggle.tsx`：`NEXT` map 为 `system → light → dark → system` 循环 ✓ UI 层三态完整

### 6.2 用户手动验收步骤

URL：http://localhost:3000/practice（任意页面均可，顶栏均含 ThemeToggle）

| 步骤 | 操作 | 期望 localStorage.openInterviewTheme | 期望 `<html>` class | 验收 |
|---|---|---|---|---|
| 1 | 清空 storage：`localStorage.clear(); location.reload()`；OS 主题设为 dark | `null`（尚未存） | 含 `.dark`（跟随系统） | [ ] 待用户手动验收 |
| 2 | OS 主题改成 light（macOS Sys Prefs / Windows 设置） | `null` | **不**含 `.dark`（实时切） | [ ] 待用户手动验收 |
| 3 | 点击 ThemeToggle → 切到 light（图标变 Sun）；刷新页面 | `"light"` | **不**含 `.dark` | [ ] 待用户手动验收 |
| 4 | 点击 ThemeToggle → 切到 dark（图标变 Moon）；刷新页面 | `"dark"` | 含 `.dark` | [ ] 待用户手动验收 |
| 5 | 点击 ThemeToggle → 回到 system（图标变 Monitor）；OS 回 light；刷新 | `"system"` | **不**含 `.dark` | [ ] 待用户手动验收 |

## 七、375px viewport 自查

**SubAgent 无浏览器 / 无响应式渲染能力。本节标注 `[ ] 待用户手动验收`。**

### 7.1 源码级确认

- `src/features/workbench/app-sidebar.tsx`：`mobileOpen` state + `Menu` / `X` lucide-react 图标（line 6）+ `sidebarCollapsedStorageKey` 记忆 → 移动态存在 hamburger 抽屉机制 ✓
- `src/features/workbench/shell.tsx` / `app/(workbench)/layout.tsx`：已去除 `min-h-[calc(100vh-77px)]` 魔术数字（grep 0 hits）✓

### 7.2 用户手动验收步骤

1. devtools → Responsive（Ctrl/Cmd+Shift+M）→ 设为 375 × 812（iPhone 13）
2. 依次访问 7 个 feature 页面：

| URL | sidebar 塌成顶部抽屉 | 主内容区单列 / 无横向溢出 | TopBar 单行（LLM pill + ThemeToggle 不换行） | PageHeader title + actions 自适应换行 | 验收 |
|---|---|---|---|---|---|
| http://localhost:3000/practice | [ ] | [ ] | [ ] | [ ] | [ ] 待用户手动验收 |
| http://localhost:3000/qa | [ ] | [ ] | [ ] | [ ] | [ ] 待用户手动验收 |
| http://localhost:3000/questions | [ ] | [ ] | [ ] | [ ] | [ ] 待用户手动验收 |
| http://localhost:3000/review | [ ] | [ ] | [ ] | [ ] | [ ] 待用户手动验收 |
| http://localhost:3000/interviews | [ ] | [ ] | [ ] | [ ] | [ ] 待用户手动验收 |
| http://localhost:3000/import | [ ] | [ ] | [ ] | [ ] | [ ] 待用户手动验收 |
| http://localhost:3000/resume | [ ] | [ ] | [ ] | [ ] | [ ] 待用户手动验收 |

3. 用 devtools Elements → Find（Ctrl/Cmd+F）搜 `overflow-x: hidden` 或 scroll bar 出现，确认主内容区无横向滚动条。

## 八、组件 props 不兼容清单（同步到 T1.11）

基于 `src/components/ui/*.tsx` 和 `src/components/workbench/*.tsx` 的新签名，对照原有调用点 grep 结果：

### 8.1 `Button`（`src/components/ui/button.tsx`）

| 维度 | 变更 | 影响面 |
|---|---|---|
| `variant` | 新增 `destructive`、`link`；保留 `primary` / `secondary` / `ghost` | 调用点签名兼容（旧 3 variant 仍生效） |
| `size` | **新增** `"sm" \| "md" \| "lg" \| "xl"`，默认 `md` = `h-[34px]` (34px) | **密度收敛**：features/ 下 **114 处 Button 调用，32 个文件，0 处显式传 `size`** → 全部从旧 `h-11`=44px 收缩到 34px。**这是有意的密度收敛（design.md §3.3 "Header 紧凑 Body 平衡"），非 break**；若某处旧设计依赖 44px 视觉权重（如 PageHeader 主 CTA），在 M2 显式加 `size="lg"` 或 `"xl"` |
| 几何 | 所有 variant 统一 `1px border`（旧 primary 无 border，次按钮有 border） | 同排按钮 height 严格一致，解决"按钮几何不对齐"（roadmap §4.4 checkbox） |
| `href` | 保留（走 `<Link>`） | 兼容 |
| 默认 `variant` | 旧代码未显式传时的视觉可能变化 | 新默认 `secondary`；旧调用点若依赖"无 variant = primary"需审阅（grep 命中 114 处需逐一看）— 但 TypeScript 严格模式保证不会静默失败 |

评估：typecheck 0 errors → 所有 114 处签名层面兼容；视觉层面默认 size 收缩属预期。**无阻塞**。

### 8.2 `Badge`（`src/components/ui/badge.tsx`）

| 维度 | 变更 | 影响面 |
|---|---|---|
| `tone` | 新增 `destructive` / `info`；`accent` 保留为 `brand` 的别名（`solidMap` / `softMap` 中显式定义两条同样的 entry） | 向下兼容：grep `tone="accent"` 命中 **27 处 / 13 文件**（qa 7、import 2、practice 6、questions 2、review 4、interviews 2、resume 3），全部继续按 brand 色渲染 |
| `variant` | **新增** `"solid" \| "soft"`，默认 `soft` | 无调用点显式传 `variant`（grep 0 hits），全部采用 soft 风格；旧 `accent` Badge 在"solid"风格下是强彩填白字，新 soft 默认变为 `brand-soft` 背景 + `brand` 文字（更克制）。**视觉变化**但非 break |
| `uppercase` 强制 | **取消**（旧版 Badge 强制 uppercase） | grep 确认 features/ 中无 `<Badge>` 内层依赖 uppercase 的情况（所有 `uppercase` 出现在 `<p>` / `<summary>` / `<div>` 上，17 处命中与 Badge 无关）；若日后某处需要大写，需在 className 显式加 `"uppercase"` |

评估：**无阻塞**。视觉上 Badge 从"强彩标签"变"弱彩标签"，符合 design.md §3.2 克制优先。

### 8.3 `SurfaceCard`（`src/components/ui/surface-card.tsx`）

| 维度 | 变更 | 影响面 |
|---|---|---|
| 实现 | 改为 `Card` 的薄 wrapper（+ 默认 `p-5`） | 现有调用点视觉略变：p-5 默认；若旧代码在 `className` 中自行覆盖 padding，用 `cn()` 后者优先生效 |
| `muted` | 保留 | 透传给 `Card`，表现与新 Card.muted 一致 |
| **`interactive`** | **保留但降级为 no-op**（JSDoc `@deprecated`） | grep features/ 无命中 `interactive={true}`（视觉依赖全部走 className "interactive-card" 全局类，该类已删，见第三节 a 项 10 处） |
| 向下兼容 | TypeScript 签名不变 | typecheck 0 errors 已验证 |

评估：**无 break；已登记 10 处 cosmetic 退化（M2 领地）**。

### 8.4 `PageHeader`（`src/components/workbench/page-header.tsx`）

| 维度 | 变更 | 影响面 |
|---|---|---|
| **`eyebrow`** | **保留但忽略**（`@deprecated`；函数体内未 destructure，亦未渲染） | grep features/ 命中 **1 处**（`src/features/qa/qa-workbench.tsx:57`），旧代码传入 `eyebrow="Grounded QA"`，新 PageHeader 不渲染，design.md §5.3 去掉 eyebrow，属预期行为 |
| `highlights` | 保留 signature（`Array<{label, value, meta?}>`），行为改：不再渲染 grid badge，改走 `StatsRow` 自动渲染 | 所有传 `highlights` 的调用点视觉节奏变化，内容仍可见 |
| `title` / `actions` / `className` | 保留 | 兼容 |
| 装饰层 | 砍掉渐变背景 / 大标题 / 4 列 grid → 替换为 `rounded-lg + border + px-4 py-3` 紧凑条 | 首屏重量下降（design.md §3.3 Header 紧凑） |

评估：**1 处 `eyebrow=` 调用点将被静默忽略，属预期；无 break。**

### 8.5 其他原子组件新签名（仅列差异要点，无不兼容）

| 组件 | 变更 | 是否兼容 |
|---|---|---|
| `Input` / `Textarea` / `Select` | T1.5 重写，仅视觉收敛（统一 border / radius-md / focus ring 用 `--color-brand`） | ✓ 签名兼容 |
| `Skeleton` | `@keyframes skeleton-pulse`（globals.css line 145-149），`surface-subtle` 色 token | ✓ 新组件；features/ 仅 2 处 `<Skeleton>` 引用，typecheck 0 errors |
| `Card` / `CardHeader` / `CardBody` / `CardFooter` | 新增拆分（T1.6） | ✓ 新导出；SurfaceCard 内部消费 |
| `Dialog` / `Tooltip` / `Separator` | 新增（T1.6） | ✓ 新组件，无旧签名冲突 |
| `ThemeToggle` | 新增（T1.3） | ✓ 新组件 |
| workbench atoms（`SectionHeading` / `EmptyList` / `DetailGrid` / `FormField` / `PlaceholderTable` / `StatsRow`） | T1.7 重写 | typecheck 0 errors → 现有调用签名兼容 |

## 九、结论

**总体验收：带已知 M2 cosmetic 退化通过（Phase 1 可独立上线）**

### 9.1 Phase 1 验收 7 条 checkbox 逐条打分

| checkbox（`roadmap.md` §4.4） | 判定 | 证据 |
|---|---|---|
| 1. `db:init + typecheck + lint + build` 全部通过 | **PASS** | 第一节构建验证 5/5 |
| 2. 7 个 feature 页面渲染无崩坏 | **PASS（待用户手动验收视觉 diff）** | 源码层 typecheck + 测试 83/83 通过；视觉层由用户按第三节 checklist 手动核验；**已知 M2 cosmetic 退化已显式登记（16+ 处 + 430 处旧 token 类），按 R-M1-06 "已知取舍"不阻塞** |
| 3. 任一排按钮跨 variant 实测高度差 ≤ 0 | **源码层 PASS（待用户手动 getBoundingClientRect 实测）** | 第四节 4.1 静态约束证明；4.2 用户手动步骤 |
| 4. 颜色 / 字体 / 圆角 / 阴影 / 表面层级 tokens 浅色 + 深色两套完整可用 | **PASS** | 第二节 2.4 + 第五节 5.1，30+ tokens 双态齐全 |
| 5. 主题切换器三态 + localStorage 记忆 | **源码层 PASS（待用户手动点击 + 刷新验证）** | 第六节 6.1 源码审阅 + 6.2 用户 checklist；`tests/theme-toggle.test.tsx` 2 tests PASS |
| 6. 375px viewport sidebar 塌抽屉，主区域不溢出 | **源码层 PASS（待用户 responsive mode 手动验收）** | 第七节 7.1 源码有 mobileOpen + Menu / X；7.2 用户 checklist |
| 7. `body` radial gradient / `.reveal-list` / `.interactive-card` / `--surface-nav` / IBM Plex 等"真的删除"（git diff 可见删除） | **PASS** | 第二节 2.1 / 2.2 / 2.3 grep CLEAN；globals.css 不再定义这些类与 token |

### 9.2 交由 T1.10 buffer

- **无阻塞问题**。T1.10 可保留 "BUFFER: no issues encountered" 占位并关闭。
- 预留给用户手动验收的 5 组 checkbox（第三 / 四 / 五 / 六 / 七节的 `[ ] 待用户手动验收`）需要用户跑一遍浏览器；若其中任意一项发现 block，再回填 T1.10。

### 9.3 交由 T1.11 delivery

下列清单需在 T1.11 `M1 完成报告` / `组件 API 变更清单` / `design-system 入口索引`中落稿：

1. **组件 props 变更（第八节）**：
   - Button：新增 `size`（default `md`=34px），5 variant 几何对齐；114 处调用点隐式收缩
   - Badge：新增 `variant`（default `soft`），`accent` = `brand` 别名；去掉强制 uppercase；27 处 `tone="accent"` 继续兼容
   - SurfaceCard：`interactive` 降级 no-op；10 处依赖 hover 的调用点登记为 M2 待修
   - PageHeader：`eyebrow` 降级 no-op（1 处调用点 `qa-workbench.tsx:57` 被静默忽略）
2. **M2 cosmetic 退化清单（第三节 3.1）**：
   - `.interactive-card` hover 失效 10 处
   - `.reveal-list` 入场动画失效 5 处
   - `@keyframes radar-draw` 失效 1 处（practice 雷达图）
   - `border-border-strong` / `text-text-muted` 等旧 token 类名 430 处 / 37 文件
   - `qa-workbench-shell.tsx:397` 内联 radial-gradient 1 处
3. **build NFT warning（第一节）**：记入"已知非阻塞"，独立技术债
4. **design-system 入口索引**：T1.1–T1.8 全部 handoff 已 complete；`globals.css` + `src/components/ui/*` + `src/components/workbench/*` 为规范落点

---

**Phase 1 可合并条件：**
- [x] 本报告 9 节齐全
- [x] 构建 / 测试 5/5 PASS
- [x] 废弃资源 grep CLEAN
- [x] 组件 props 清单完整
- [ ] 用户完成第三 / 四 / 五 / 六 / 七节的手动验收 checklist，确认无 block → 回填本报告对应 [ ] 为 [x]
- [ ] T1.10 buffer 按用户验收结果确认"无问题"并关闭
- [ ] T1.11 delivery 文档基于本报告第 9.3 节落稿

<!-- handoff: H10 · status: complete · timestamp: 2026-04-15 17:35 -->
