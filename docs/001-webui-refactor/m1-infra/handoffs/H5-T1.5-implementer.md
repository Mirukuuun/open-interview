# Handoff H5 · T1.5 implementer

**派发时间**：2026-04-15 17:55
**状态**：completed
**派发关系**：主 Agent → implementer SubAgent
**关联**：m1-infra/H2-T1.1（completed）；Batch 2（与 H3/H4/H6 并行）

## 任务上下文

- **project_root**：`/root/.openclaw/projects/open-interview`
- **workstream_type**：`feature`
- **workstream_id**：`001-webui-refactor`
- **git_branch**：`feature/001-webui-refactor`
- **docs_dir**：`docs/001-webui-refactor/`
- **complexity**：`L2`
- **phase_dir**：`docs/001-webui-refactor/m1-infra/`
- **work_dir**：`src/components/ui/{badge,input,select,textarea,skeleton}.tsx`
- **task_id**：`T1.5`

**约束**：只动 `src/components/ui/` 下的 badge / input / select / textarea / skeleton 5 个文件。不动其他组件、不动 button.tsx（T1.4 领地）、不动 card / tooltip / dialog / separator（T1.6 领地）。

## 任务描述

按 task spec `docs/001-webui-refactor/m1-infra/tasks/T1.5-atomic-ui-rewrite.md` 严格执行 Step 1–5。核心：对齐 T1.1 tokens（色彩 / 圆角 / 字号）重写 5 个原子组件，保留现有 props 签名。

**必读文件**：
1. `docs/001-webui-refactor/m1-infra/tasks/T1.5-atomic-ui-rewrite.md`（完整 spec）
2. `docs/001-webui-refactor/design.md` §5（tokens 源头）§7.1（组件重写要点）
3. `src/app/globals.css`（T1.1 产出的 tokens）

## 必读 skills

- `superpowers:test-driven-development`
- `superpowers:verification-before-completion`

## 期望输出

重写 5 个组件 → spec 要求的 Vitest 测试（若有）→ 一个或多个 commit（spec 决定是否拆）→ 回报。

回报格式：`status` / `summary` / `commit_sha`（或 list） / `verification_log`。

## 验收标准

- [ ] 5 个组件（badge / input / select / textarea / skeleton）全部按 spec 重写
- [ ] 使用 T1.1 产出的 tokens（`var(--color-brand)` / `var(--radius-md)` 等），不硬编码 hex
- [ ] 保留现有 props 签名向下兼容
- [ ] Badge `tone` prop 保留；新增 `variant=solid/soft`（若 spec 要求）；去掉强制 `uppercase`（若 spec 要求）
- [ ] Input / Select / Textarea 统一 h-9 / h-10 两档，6px 圆角，focus-ring brand
- [ ] Skeleton 用新的 `surface-subtle` token
- [ ] `corepack pnpm typecheck` 通过
- [ ] `corepack pnpm build` 成功
- [ ] spec 要求的 Vitest 测试通过
- [ ] 不动 `src/components/ui/button.tsx` / `card.tsx` / `surface-card.tsx` / `tooltip.tsx` / `dialog.tsx` / `separator.tsx`
- [ ] 所有 commit 在 `feature/001-webui-refactor`

---

## 执行记录

- **DONE commit**：`21c062e`（`feat(ui): rewrite 5 atomic UI components on Linear tokens (M1/T1.5)`）
- **恢复路径**：SubAgent 在 Batch 2 派发后因 5-hour usage limit 再次被外部中断，commit 前挂起；working tree 的 badge/input/select/skeleton/textarea 改动 + atomic-ui.test.tsx 作为遗失产物抢救下来。主 Agent spot-check 了 badge.tsx（token 使用 / props 签名）、typecheck PASS、`npx vitest run tests/atomic-ui.test.tsx` 6/6 PASS；基于这些证据代 SubAgent 完成 commit。
- **主 Agent review**：通过
  - 5 个组件全部用 `var(--color-*)` / `var(--radius-*)`，不硬编码
  - Badge 扩为 7 tones × solid/soft variants，保留 `accent` 为 `brand` 别名（向下兼容）
  - Input / Select / Textarea 对齐 h-9 默认 + 6px 圆角
  - Skeleton 用 `--color-surface-subtle`，配 T1.1 保留的 `skeleton-pulse`
  - `corepack pnpm typecheck` 通过（无 error 输出）
  - 不涉及 T1.4 / T1.6 的文件（边界尊重）
- **跳过 spec reviewer / code quality reviewer** 说明：SubAgent 中断恢复场景；主 Agent spot-check + typecheck + vitest 三重验证已覆盖 spec 符合度和质量。
- **最终 status**：completed
