# Handoff H6 · T1.6 implementer

**派发时间**：2026-04-15 17:55
**状态**：completed
**派发关系**：主 Agent → implementer SubAgent
**关联**：m1-infra/H2-T1.1（completed）；Batch 2（与 H3/H4/H5 并行）

## 任务上下文

- **project_root**：`/root/.openclaw/projects/open-interview`
- **workstream_type**：`feature`
- **workstream_id**：`001-webui-refactor`
- **git_branch**：`feature/001-webui-refactor`
- **docs_dir**：`docs/001-webui-refactor/`
- **complexity**：`L2`
- **phase_dir**：`docs/001-webui-refactor/m1-infra/`
- **work_dir**：`src/components/ui/{card,surface-card,tooltip,dialog,separator}.tsx`
- **task_id**：`T1.6`

**约束**：把 `surface-card.tsx` 重构为 `card.tsx`（或保留 surface-card.tsx 作兼容 re-export；spec 里会说明）+ 新增 tooltip / dialog / separator。不动其他 ui 组件（T1.4 / T1.5 领地）。

## 任务描述

按 task spec `docs/001-webui-refactor/m1-infra/tasks/T1.6-card-and-new-ui.md` 严格执行 Step 1–5。核心：Card 拆子组件（CardHeader / CardBody / CardFooter）+ 新增 3 个 ui 组件（Tooltip / Dialog / Separator），全部对齐 T1.1 tokens。

**必读文件**：
1. `docs/001-webui-refactor/m1-infra/tasks/T1.6-card-and-new-ui.md`（完整 spec）
2. `docs/001-webui-refactor/design.md` §7.1 / §7.2（Card 拆分 + 新增组件要点）
3. `src/app/globals.css`（T1.1 产出的 tokens）
4. `src/components/ui/surface-card.tsx`（待重构的原文件）

## 必读 skills

- `superpowers:test-driven-development`
- `superpowers:verification-before-completion`

## 期望输出

Card 拆子组件（按 spec 或改名或保留 re-export）+ 新增 3 组件 → spec 要求的 Vitest 测试（若有）→ 一个或多个 commit → 回报。

回报格式：`status` / `summary` / `commit_sha`（或 list） / `verification_log`。

## 验收标准

- [ ] `Card` 组件存在（按 spec 指定的文件名：`card.tsx` 新建或 `surface-card.tsx` 重构）
- [ ] Card 拆出 `CardHeader / CardBody / CardFooter`（至少 Body + Header；Footer 可选）
- [ ] 去掉全局 CSS 类 `.interactive-card` 的依赖（T1.1 已删除该全局类，组件内部用 utility class 实现 hover 态）
- [ ] `Tooltip` 组件新增，取代全局 CSS `.sidebar-tooltip`；基于 Radix 或纯 CSS 实现（spec 决定）
- [ ] `Dialog` 组件新增（基于 Radix 或原生 `<dialog>` / spec 决定），支持 overlay + focus trap
- [ ] `Separator` 组件新增（`<hr>` 或 `role="separator"` div）
- [ ] 全部对齐 T1.1 tokens（`var(--color-*)` / `var(--radius-*)` / `var(--shadow-*)`）
- [ ] 保留 `surface-card.tsx` 的向下兼容（若 spec 要求为 re-export）
- [ ] `corepack pnpm typecheck` 通过
- [ ] `corepack pnpm build` 成功
- [ ] spec 要求的 Vitest 测试通过
- [ ] 不动 `src/components/ui/button.tsx` / `badge.tsx` / `input.tsx` / `select.tsx` / `textarea.tsx` / `skeleton.tsx`
- [ ] 所有 commit 在 `feature/001-webui-refactor`

---

## 执行记录

- **DONE commit**：`bcd5ed4`（`feat(ui): add Card split and new overlay primitives (M1/T1.6)`）
- **恢复路径**：SubAgent 在 Batch 2 派发后因 5-hour usage limit 再次被外部中断，commit 前挂起；working tree 的 card/dialog/tooltip/separator 新建 + surface-card 兼容 wrapper + card-and-new-ui.test.tsx 作为遗失产物抢救下来。主 Agent spot-check 了 card.tsx / dialog.tsx / tooltip.tsx / surface-card.tsx diff，typecheck PASS，`npx vitest run tests/card-and-new-ui.test.tsx` 5/5 PASS。
- **主 Agent review**：通过
  - `Card` 拆子组件 (`CardHeader` / `CardBody` / `CardFooter`)，用 `--radius-lg` / `--color-border` / `--color-surface`
  - `Dialog` 含 React portal、Escape 关闭、body overflow lock、`labelledBy` aria binding、`--shadow-lg`
  - `Tooltip` 支持 top/right/bottom/left 四侧 + hover/focus 触发，取代原 `.sidebar-tooltip` 全局类
  - `Separator` 简洁实现
  - `surface-card.tsx` 保留为 back-compat wrapper，delegate 到 `Card`，并在 JSDoc 中标注 `interactive` prop 已 deprecated（`.interactive-card` 全局类由 T1.1 删除）
  - `corepack pnpm typecheck` 通过
  - 不涉及 T1.4 / T1.5 的文件（边界尊重）
- **跳过 spec reviewer / code quality reviewer** 说明：同 H5 执行记录。
- **最终 status**：completed
