# Handoff H9 · T1.8 implementer

**派发时间**：2026-04-15 19:30
**状态**：completed
**派发关系**：主 Agent → implementer SubAgent
**关联**：H7-T1.3 / H8-T1.7 （both completed）；最后一个开发类 task

## 任务上下文

- **project_root**：`/root/.openclaw/projects/open-interview`
- **workstream_type**：`feature`
- **workstream_id**：`001-webui-refactor`
- **git_branch**：`feature/001-webui-refactor`
- **docs_dir**：`docs/001-webui-refactor/`
- **complexity**：`L2`
- **phase_dir**：`docs/001-webui-refactor/m1-infra/`
- **work_dir**：`src/features/workbench/{app-sidebar,top-bar,shell}.tsx` + `src/app/(workbench)/layout.tsx`
- **task_id**：`T1.8`

**约束**：只动 `src/features/workbench/` 下的 app-sidebar / top-bar / shell 三个文件 + `src/app/(workbench)/layout.tsx`。不动 `src/app/layout.tsx`（T1.3 已处理 ThemeProvider + bootstrap script）。不动 feature 页面（`src/features/{import,review,questions,practice,interviews,qa,resume}/*`，M2 领地）。不动 `src/components/ui/*`（消费方，T1.4/T1.5/T1.6/T1.3 产出）。

## 任务描述

按 task spec `docs/001-webui-refactor/m1-infra/tasks/T1.8-shell-refactor.md` 严格执行 Step 1-5。核心重构：

**app-sidebar.tsx**：从深色靛紫 (`bg-surface-nav text-text-inverse`) 改为浅色 (`surface-muted + foreground`)；去掉 4 色方块 logo / 渐变 logo 卡片 / inset shadow；导航项改用 `hover:bg-zinc-100 / active:bg-zinc-200 / 选中 bg-brand-soft text-brand`；保留折叠记忆；移动端 overflow tab 改为顶部抽屉（sidebar 塌成 hamburger 触发的 Drawer）。使用 T1.6 新建的 `Tooltip` 组件取代全局 `.sidebar-tooltip` CSS。

**top-bar.tsx**：砍 eyebrow + "工作台" + "本地优先" 冗余；只保留当前页面面包屑/页标题 + **ThemeToggle**（T1.3 新建）+ LLM provider 状态 pill；`sticky top-0 backdrop-blur` 保留；border-b 用 token。

**shell.tsx**：去 `min-h-[calc(100vh-77px)]` 魔术数字（改 flex 正确撑满）；`max-w-[1680px]` 保留；间距按密度策略（body 平衡档 padding 调整）。

**(workbench)/layout.tsx**：sanity pass，确认和 root layout 协作正常。

**必读文件**：
1. `docs/001-webui-refactor/m1-infra/tasks/T1.8-shell-refactor.md`（完整 spec）
2. `docs/001-webui-refactor/design.md` §8（Shell 层重构）§5.2（深色模式）
3. `src/app/globals.css`（tokens）
4. `src/components/ui/theme-toggle.tsx`（T1.3 产物，top-bar 要消费）
5. `src/components/ui/tooltip.tsx`（T1.6 产物，sidebar 要消费替换 `.sidebar-tooltip`）
6. `src/features/workbench/route-definitions.ts`（导航项定义，readonly）

## 必读 skills

- `superpowers:test-driven-development`
- `superpowers:verification-before-completion`

## 期望输出

重构 3 个 workbench 文件 + `(workbench)/layout.tsx` + 对应 Vitest 测试（若 spec 要求）→ 一个或多个 commit → 回报。

回报格式：`status` / `summary` / `commit_sha`（或 list） / `verification_log`。

## 验收标准

- [ ] `app-sidebar.tsx`：浅色底 + 深文字；去 4 色方块 logo / 渐变卡；选中态 `bg-brand-soft text-brand`；保留折叠（`localStorage: open-interview:sidebar-collapsed`）；使用 `Tooltip` 组件替代全局 `.sidebar-tooltip`；移动端塌成顶部抽屉（hamburger 触发）
- [ ] `top-bar.tsx`：砍冗余 eyebrow + "工作台" + "本地优先"；右侧加 `<ThemeToggle />`；保留 LLM provider 状态 pill；`sticky top-0 backdrop-blur` 保留
- [ ] `shell.tsx`：去魔术数字 `min-h-[calc(100vh-77px)]`；用 `flex` + `min-h-screen` 撑满；间距按密度策略
- [ ] `(workbench)/layout.tsx`：确保 WorkbenchShell 正常挂载，无 breaking
- [ ] 全部使用 T1.1 tokens（`var(--color-*)`）+ 消费 T1.4/T1.5/T1.6/T1.3 的新组件
- [ ] `corepack pnpm typecheck` 通过
- [ ] `corepack pnpm lint` 通过
- [ ] `corepack pnpm build` 成功
- [ ] `corepack pnpm test` 全量 PASS（78+ tests，无 regression；spec 可能要求新增 shell 相关测试）
- [ ] 不动 `src/app/layout.tsx` / `src/app/globals.css` / `src/components/ui/*` / `src/features/*/` 的 feature 代码
- [ ] 所有 commit 在 `feature/001-webui-refactor`

---

## 执行记录

- **DONE commit**：`ba89ed3`
- **Implementer model**：sonnet
- **主 Agent review**：通过
  - AppSidebar 深色→浅色（`bg-[color:var(--color-surface-muted)]`），选中态 `brand-soft`，Tooltip 组件替换全局 `.sidebar-tooltip` CSS，保留折叠 localStorage，移动端塌为顶部抽屉 ✓
  - TopBar 砍 eyebrow + "工作台" + "本地优先"，右侧加 `<ThemeToggle />`，LLM pill tokens 化，sticky backdrop-blur 保留 ✓
  - Shell 移除 `min-h-[calc(100vh-77px)]` 魔术数字改 flex min-h-screen ✓
  - `(workbench)/layout.tsx` sanity pass（无需改动）
  - 新增 `tests/shell-smoke.test.tsx` 5 断言守护 deprecated classes/text
  - typecheck / lint / build 全绿；全量 83/83 tests 通过
- **最终 status**：completed
