# Handoff H4 · T1.4 implementer

**派发时间**：2026-04-15 17:55
**状态**：completed
**派发关系**：主 Agent → implementer SubAgent
**关联**：m1-infra/H2-T1.1（completed）；Batch 2（与 H3/H5/H6 并行）

## 任务上下文

- **project_root**：`/root/.openclaw/projects/open-interview`
- **workstream_type**：`feature`
- **workstream_id**：`001-webui-refactor`
- **git_branch**：`feature/001-webui-refactor`
- **docs_dir**：`docs/001-webui-refactor/`
- **complexity**：`L2`
- **phase_dir**：`docs/001-webui-refactor/m1-infra/`
- **work_dir**：`src/components/ui/button.tsx`
- **task_id**：`T1.4`

**约束**：只动 `src/components/ui/button.tsx`。保持现有 props 兼容（`variant / href / children`），新增 `size` prop 默认值等价于现有行为（见 spec）。不动其他文件。

## 任务描述

按 task spec `docs/001-webui-refactor/m1-infra/tasks/T1.4-button-rewrite.md` 严格执行 Step 1–5。核心：重写 `Button` 组件为 5 variant × 4 size 矩阵，全部 1px border 几何对齐，保留现有 props 签名向下兼容。

**必读文件**：
1. `docs/001-webui-refactor/m1-infra/tasks/T1.4-button-rewrite.md`（完整 spec）
2. `docs/001-webui-refactor/design.md` §6（按钮规范源头，spec 已摘取）
3. `src/app/globals.css`（T1.1 产出的 tokens，需要在组件里 consume）

## 必读 skills

- `superpowers:test-driven-development`
- `superpowers:verification-before-completion`

## 期望输出

重写 `src/components/ui/button.tsx` → 按 spec 要求可能新增一个 Vitest 测试文件 → 一个 commit（按 spec 模板）→ 回报。

回报格式：`status` / `summary` / `commit_sha` / `verification_log`。

## 验收标准

- [ ] `Button` 支持 5 variant：`primary / secondary / ghost / destructive / link`
- [ ] `Button` 支持 4 size：`sm / md / lg / xl`（对应 28 / 34 / 40 / 46px 高）
- [ ] 所有 variant 都有 1px border（`transparent` 或同色），保证几何对齐
- [ ] Link variant 不自动加箭头
- [ ] 保留现有 props 签名（`variant / href / children`），不 breaking
- [ ] 新增 `size` prop 默认值选择尽量贴近原 `h-11` 表现（可用 md=34 作新默认并在 commit message 中注明"有意密度收敛"）
- [ ] icon-only 用法可达（正方形，边长 = size 高度；`aria-label` 必须）
- [ ] `corepack pnpm typecheck` 通过
- [ ] `corepack pnpm build` 成功
- [ ] spec 要求的 Vitest 测试（若有）通过
- [ ] 一个 commit 在 `feature/001-webui-refactor`

---

## 执行记录

- **DONE commit**：`a60950f`（`feat(ui): rewrite Button with 5 variants x 4 sizes geometric alignment (M1/T1.4)`）
- **Implementer model**：sonnet
- **主 Agent review**：通过（11/11 验收 ✓）
  - `ButtonVariant` 5 个：primary / secondary / ghost / destructive / link ✓
  - `ButtonSize` 4 档：sm (h-7≈28) / md (h-[34px]) / lg (h-10=40) / xl (h-[46px]) ✓
  - 非 link variant 全带 1px border（透明或同色），几何对齐 ✓
  - link variant 无 border / 无 auto-arrow（design.md §6 允许的唯一几何例外）
  - 保留 `variant / href / children` props，新增可选 `size`（默认 md）✓
  - `corepack pnpm typecheck` / `lint` / `build` 均 PASS
  - `npx vitest run tests/button.test.tsx`：6/6 PASS
  - 全量 test：68/68 PASS（含 atomic-ui / card-and-new-ui / button）
- **合理 side-effect**：implementer 将 `jsdom` + `@testing-library/react` 从 transitive deps 显式提升为 devDependencies，修复 T1.5/T1.6 jsdom-based 测试的 pnpm strict-deps 隐患。轻微 scope creep 但合理，已在 commit message 中说明。
- **最终 status**：completed
