# Handoff H2 · T1.1 implementer

**派发时间**：2026-04-15 17:35
**状态**：completed
**派发关系**：主 Agent → implementer SubAgent
**关联**：m1-infra/H1-planning-phase（completed），为 plan.md Batch 1 的唯一 task

## 任务上下文（SubAgent 必读 + 严格遵守）

- **project_root**：`/root/.openclaw/projects/open-interview`
- **workstream_type**：`feature`
- **workstream_id**：`001-webui-refactor`
- **git_branch**：`feature/001-webui-refactor`
- **docs_dir**：`docs/001-webui-refactor/`
- **complexity**：`L2`
- **phase_dir**：`docs/001-webui-refactor/m1-infra/`
- **work_dir**：`src/app/globals.css`（本 task 唯一修改的源文件；docs 下不产出新文档）
- **task_id**：`T1.1`

**约束**：
- 只改 `src/app/globals.css`，**不碰**任何其他源文件（字体 import 由 T1.2 在 `layout.tsx` 里处理）
- 不创建任何新文档（本 task 是 development 类，产物是代码）
- git branch 必须仍是 `feature/001-webui-refactor`

## 任务描述

按 task spec `docs/001-webui-refactor/m1-infra/tasks/T1.1-tokens-globalcss-rewrite.md` 严格执行 Step 1–Step 5。

**核心**：把 `src/app/globals.css` 完全重写成 Linear 派 design tokens 体系（颜色 / 字体栈 / 圆角 / 阴影 / 表面层级），含浅色默认 + `.dark` 深色变体双态；同时砍掉所有废弃 CSS（radial gradient / interactive-card / reveal-list / fade-in-up / radar-draw / sidebar-tooltip / --surface-nav / IBM Plex import / selection 靛紫）。

**task spec 已给出**：
- Step 1-2 的 FAIL 验证命令集（grep + build）
- Step 3 的完整 CSS 代码模板（约 200 行，可直接作为实现）
- Step 4 的 PASS 验证命令集
- Step 5 的 commit 消息模板

implementer 照 spec 走即可，不需要自己设计 tokens。

**必读文件**（按优先级）：

1. `docs/001-webui-refactor/m1-infra/tasks/T1.1-tokens-globalcss-rewrite.md`（本 task 的完整 spec）
2. `src/app/globals.css`（当前状态，用于对比验证"砍掉"项）

**参考文件**（可选）：

- `docs/001-webui-refactor/design.md` §5（全局规范原始决策，spec 已摘取）

## 必读 skills

- `superpowers:test-driven-development` — 按 Step 1→2→3→4→5 TDD 流程执行
- `superpowers:verification-before-completion` — 报告 DONE 前必须先跑 Step 4 验证命令并确认全 PASS

## 期望输出

**落盘文件**：

- `src/app/globals.css`（重写后的文件）

**commit**：

- 一个 commit，消息模板在 task spec Step 5 末尾。commit 可以直接用 spec 给的内容。

**回报格式**：

- `status`: `DONE` / `DONE_WITH_CONCERNS` / `BLOCKED` / `NEEDS_CONTEXT`
- `summary`: 一句话
- `commit_sha`: implementer 创建的 commit 哈希
- `verification_log`: Step 4 验证命令集的输出（关键行即可，尤其是 build PASS / 废弃 token 清 CLEAN 的证据）
- `concerns`（如有）

## 验收标准（双向：implementer DONE 前自检 + 主 Agent review 依据）

- [ ] `src/app/globals.css` 包含 task spec Step 3 列出的所有 tokens（功能色 6+6 soft / 中性别名 4 / 表面 3 / radius 4 / shadow 3 / font stack / motion tokens）
- [ ] 同文件有 `@custom-variant dark (&:where(.dark, .dark *));` 声明
- [ ] `.dark { ... }` 深色变体块存在，覆盖所有 color / shadow tokens
- [ ] `@theme inline { ... }` 映射到 Tailwind v4 工具类
- [ ] `html { color-scheme: light dark; }` 已加
- [ ] `@import "@fontsource/inter/..."` 已替换 `@fontsource/ibm-plex-sans/...`
- [ ] 保留 `@keyframes skeleton-pulse` 和 `@media (prefers-reduced-motion: reduce)`
- [ ] **砍掉**（git diff 显示"删除"而非覆盖）：body radial gradient / `.interactive-card` / `.sidebar-tooltip` / `.reveal-list > *` + `:nth-child` / `@keyframes fade-in-up` / `@keyframes radar-draw` / `--surface-nav` / `--surface-nav-muted` / selection 靛紫 rgba
- [ ] 验证 grep CLEAN：
  ```bash
  grep -E "(surface-nav|interactive-card|reveal-list|fade-in-up|radar-draw|sidebar-tooltip)" src/app/globals.css  # 应无输出
  grep -c "@fontsource/ibm-plex" src/app/globals.css  # 应为 0
  grep -c "\.dark" src/app/globals.css  # 应 ≥ 1
  grep -c "@custom-variant dark" src/app/globals.css  # 应为 1
  ```
- [ ] `corepack pnpm build` 成功（无 Tailwind v4 @theme 编译错误）
- [ ] 一个 commit 创建在 `feature/001-webui-refactor` 分支（commit message 按 spec Step 5 模板）
- [ ] 没有修改 `src/app/globals.css` 之外的任何源文件

---

## 执行记录

- **DONE 时间**：2026-04-15 16:00:24 (commit timestamp)
- **Implementer model**：haiku
- **SubAgent 回报 status**：DONE
- **commit_sha**：`9c2f17702d5e749cf1101a5b961bc8f3d06c898e`
- **git stat**：`src/app/globals.css` +106 / -161（净缩 55 行，符合"重写 + 清理"预期）
- **主 Agent review**：通过（14/14 验收标准 ✓）
  - grep 废弃 token（surface-nav / interactive-card / reveal-list / fade-in-up / radar-draw / sidebar-tooltip / ibm-plex）全部 "no matches found"
  - grep 新增必备项（@custom-variant dark / .dark { / --color-brand / --radius-md / skeleton-pulse / color-scheme）全部命中
  - `@theme inline` 映射齐全
  - commit message 符合 task spec Step 5 模板
- **跳过 spec reviewer / code quality reviewer** 说明：T1.1 为 mechanical CSS token 重写，单文件（`src/app/globals.css`），task spec 已给出完整代码模板（implementer 照抄即可），grep 验证可覆盖 spec 符合度，无传统 code quality 维度可审。主 Agent review 合并承担两个 reviewer 职责（行使 executing-phase skill 的"主 Agent 否决权"）。
- **最终 status**：completed
