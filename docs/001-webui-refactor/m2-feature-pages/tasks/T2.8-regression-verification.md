---
category: testing
files_owned:
  - tests/m2-regression.test.tsx
files_readonly:
  - src/features/practice/**
  - src/features/qa/**
  - src/features/questions/**
  - src/features/review/**
  - src/features/interviews/**
  - src/features/import/**
  - src/features/resume/**
  - docs/001-webui-refactor/m2-feature-pages/plan.md
  - docs/reference/design-system.md
files_shared: []
---

# T2.8 · 整体回归验证

## 目标

在 T2.1–T2.7 全部合入后，执行全量工具链验证 + 7 页视觉 diff 汇总 + 动作/路由对等自测清单，确认 M2 phase 没有引入回归。本 task 不修改 `src/*` 源代码（仅可能新增 / 调整测试文件），属于纯验证性质。

## 涉及文件

### Create / Modify（owned）

- `tests/m2-regression.test.tsx`（可选：如需增补全局 grep-absent 冒烟测试汇总；各页独立测试已由 T2.1–T2.7 产出）

### Read-only

- `src/features/{practice,qa,questions,review,interviews,import,resume}/**`（所有 feature 页面源码）
- `docs/001-webui-refactor/m2-feature-pages/plan.md`（phase 验收标准对照）
- `docs/reference/design-system.md`（规范对照）

## 验收条件

### 工具链验证

- [ ] `corepack pnpm db:init` PASS
- [ ] `corepack pnpm typecheck` 0 errors
- [ ] `corepack pnpm lint` 0 warnings
- [ ] `corepack pnpm build` 成功（7 个 feature 路由全部生成）
- [ ] `corepack pnpm test`（Vitest 全量）PASS，0 failures

### 全局 grep-clean 验证

对 `src/features/**` 全局 grep，以下所有模式命中 0：

- [ ] `interactive-card` 命中 0
- [ ] `reveal-list` 命中 0
- [ ] `radar-draw` 命中 0
- [ ] `border-border-strong` 命中 0
- [ ] `text-text-muted` 命中 0
- [ ] `text-text-strong` 命中 0
- [ ] `border-border-muted` 命中 0
- [ ] `bg-accent`（word boundary）命中 0
- [ ] `text-accent`（word boundary）命中 0
- [ ] `eyebrow=` 命中 0
- [ ] `radial-gradient`（`src/features/**`）命中 0

### 7 页视觉 diff 汇总

每页一行确认（由执行人在浏览器中对照重构前后截图）：

- [ ] `/practice` 三段布局成立，动作/路由对等
- [ ] `/qa` 聊天气泡 + 会话抽屉成立，动作/路由对等
- [ ] `/questions` 保留骨架 + 新密度成立，动作/路由对等
- [ ] `/review` 保留骨架 + 新密度成立，动作/路由对等
- [ ] `/interviews` 左右分栏成立，动作/路由对等
- [ ] `/import` 表单分步成立，动作/路由对等
- [ ] `/resume` 布局成立，动作/路由对等

### 页面间节奏差异化验证

- [ ] practice / qa / questions 三类有可感知的布局差异（phase 验收条件）
- [ ] 统一遵循"一个语境一个 Primary"（逐页截图确认）
- [ ] 所有按钮 / input / badge 使用 M1 新组件，无 inline 样式 hack

### 响应式验证

- [ ] 375px viewport 下 7 个页面均不溢出，sidebar 塌成抽屉
- [ ] 深色模式切换下 7 个页面无视觉崩坏

## 依赖

- **前置**：T2.1–T2.7 全部合入
- **后置**：T2.10（buffer 收敛）→ T2.9（delivery closing）

## 估算

0.5 天（纯验证，不改源代码）

## 执行步骤

### Step 1 · 工具链全量运行

```bash
corepack pnpm db:init
corepack pnpm typecheck
corepack pnpm lint
corepack pnpm build
corepack pnpm test
```

逐条确认 0 errors / 0 warnings / 0 failures。记录各命令输出摘要到验收条件 checkbox。

### Step 2 · 全局 grep-clean 验证

```bash
grep -rn 'interactive-card' src/features/ | wc -l
grep -rn 'reveal-list' src/features/ | wc -l
grep -rn 'radar-draw' src/features/ | wc -l
grep -rn 'border-border-strong' src/features/ | wc -l
grep -rn 'text-text-muted' src/features/ | wc -l
grep -rn 'text-text-strong' src/features/ | wc -l
grep -rn 'border-border-muted' src/features/ | wc -l
grep -rn '\bbg-accent\b' src/features/ | wc -l
grep -rn '\btext-accent\b' src/features/ | wc -l
grep -rn 'eyebrow=' src/features/ | wc -l
grep -rn 'radial-gradient' src/features/ | wc -l
```

全部 0 则通过；非 0 则记录残留位置，判断是否归 T2.10 buffer 处理。

### Step 3 · 7 页视觉 diff + 动作对等自测

在浏览器中逐页检查（`http://localhost:3000/<page>`）：
- 截图重构后页面
- 对照重构前截图（或记忆中的旧布局）确认布局变化符合 design note
- 逐条走验收条件中的"手动自测"清单
- 在 375px viewport 和深色模式下各重复一遍

### Step 4 · 记录结果

将所有 checkbox 勾选结果记录到本 task 文件或独立的 regression report。若发现回归问题，记录到 T2.10 buffer。

### Step 5 · commit（仅当新增/调整了测试文件）

```bash
git add tests/m2-regression.test.tsx
git commit -m "test(ui): add M2 regression verification sweep (M2/T2.8)"
```

若无新文件产出则不需 commit。

<!-- handoff: H1 · status: complete · timestamp: 2026-04-16 09:30 -->
