# Handoff H4 · T2.2 fix (qa page layout restructuring)

**派发时间**：2026-04-16 11:45
**状态**：completed
**派发关系**：主 Agent → fix SubAgent
**关联**：H3-T2.2-implementer（superseded — cleanup done, layout missing）

## 任务上下文

- **project_root**：`/root/.openclaw/projects/open-interview`
- **workstream_type**：`feature`
- **workstream_id**：`001-webui-refactor`
- **git_branch**：`feature/001-webui-refactor`
- **docs_dir**：`docs/001-webui-refactor/`
- **complexity**：`L2`
- **phase_dir**：`docs/001-webui-refactor/m2-feature-pages/`
- **work_dir**：`docs/001-webui-refactor/m2-feature-pages/`

**约束**：
- 代码改动限制在 `src/features/qa/*`
- **禁止修改** `src/components/*`、`src/app/*`、其他 feature 目录
- 不动业务逻辑（CRUD / AI 调用 / streaming / server calls 不变）
- 视觉规范：`docs/reference/design-system.md`

## 任务描述

H3 已完成 T2.2 的 M1 cosmetic cleanup（所有 grep 验收通过）。本次 fix 只做 H3 遗漏的**布局重整**（task file Step 3 items 6-7）。

**必读**：`docs/001-webui-refactor/m2-feature-pages/tasks/T2.2-qa-page.md` 的 §轻量 Design Note §2（布局骨架）和 Step 3 items 6-7。

**具体目标**（2 项）：

### Item 6: 布局重整（qa-workbench-shell.tsx 主体）

当前 QA 页面的会话列表和消息区域布局需要调整为更像聊天应用的风格：

- 确保左侧会话列表区域作为 sidebar（`w-60 flex-none`，border-r 分隔）
- 确保右侧消息区域 `flex-1 min-w-0` 占满剩余空间
- 消息气泡单列排列：AI 回复 `self-start`（左对齐），用户提问 `self-end`（右对齐）
- 底部输入区 `sticky bottom-0` 挂底
- 会话卡片选中态用 `bg-brand-soft` 高亮
- **响应式**：< 768px 左侧抽屉收起为 overlay

### Item 7: PageHeader 统一

确保 QA 页面使用 `<PageHeader>` 组件（从 `@/components/workbench/page-header` 导入），紧凑档，actions 含单个 Primary "新建会话"（lg size）。如果现在 shell 用的是自定义 header，替换为 PageHeader。

**重要约束**：
- **不动业务逻辑**：所有 session CRUD、AI streaming、表单提交的逻辑保持不变
- **不动已清理的 cleanup**：H3 已把所有 deprecated token 替换完毕，不要回退
- 只改 CSS 类名和 JSX 结构（布局容器包裹、flex/grid 调整、消息气泡对齐）

## 必读 skills

- `superpowers:verification-before-completion`

## 期望输出

**代码变更**：主要改 `src/features/qa/qa-workbench-shell.tsx`，可能波及 `qa-workbench.tsx`

**commit**（fix SubAgent 完成后自行 commit）

**回报格式**：status / summary / concerns / files_changed / test_results

## 验收标准

- [ ] `corepack pnpm typecheck` 0 errors
- [ ] `corepack pnpm lint` 0 warnings/errors
- [ ] `corepack pnpm build` 成功
- [ ] `npx vitest run tests/qa-page.test.tsx` 仍 PASS（不回退 cleanup）
- [ ] git branch 仍是 `feature/001-webui-refactor`
- [ ] 已 commit

---

## 执行记录

- **DONE 时间**：2026-04-16 11:35
- **SubAgent 回报 status**：complete
- **commit**：`2ac8cc0` — `fix(ui): restructure /qa layout to sidebar+chat flex with PageHeader (M2/T2.2 items 6-7)`
- **files_changed**：`qa-workbench-shell.tsx`（82 ins / 130 del）
- **验证结果**：vitest 5/5 PASS · typecheck 0 · lint 0 · build OK
- **concern 记录**：helper functions 里有 `bg-white` 不跟踪 dark mode → out of scope for T2.2，记入 T2.10 buffer 备注
- **主 Agent review**：通过
  - PageHeader 消费确认（line 277）
  - 左侧 sidebar `w-60 flex-none`（line 288）
  - 气泡 `self-start/self-end`（line 379）
  - 输入区 `sticky bottom-0`（line 436）
  - 所有 grep 验收仍 0 ✓
- **最终 status**：completed
