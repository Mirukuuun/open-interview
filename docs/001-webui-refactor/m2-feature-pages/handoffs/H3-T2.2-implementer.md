# Handoff H3 · T2.2 implementer (qa page)

**派发时间**：2026-04-16 11:20
**状态**：superseded
**派发关系**：主 Agent → implementer SubAgent
**关联**：H2-T2.1-implementer（completed）；T2.2 task file

## 任务上下文（dev-workflow 注入，SubAgent 必读 + 严格遵守）

- **project_root**：`/root/.openclaw/projects/open-interview`
- **workstream_type**：`feature`
- **workstream_id**：`001-webui-refactor`
- **git_branch**：`feature/001-webui-refactor`
- **docs_dir**：`docs/001-webui-refactor/`
- **complexity**：`L2`
- **phase_dir**：`docs/001-webui-refactor/m2-feature-pages/`
- **work_dir**：`docs/001-webui-refactor/m2-feature-pages/`

**约束**：
- 代码改动限制在 `src/features/qa/*` + `tests/qa-page.test.tsx`
- **禁止修改** `src/components/*`、`src/app/*`、`src/features/workbench/*`（M1 领地）
- **禁止修改** 其他 feature 目录
- 所有视觉规范以 `docs/reference/design-system.md` 为唯一权威来源
- 遵循 TDD：先写失败测试 → run FAIL → 实现 → run PASS

## 任务描述

实现 T2.2：`/qa` 页面重构（聊天气泡 + 会话抽屉）。

**完整 task 规格见**：`docs/001-webui-refactor/m2-feature-pages/tasks/T2.2-qa-page.md`

**核心目标摘要**：
1. 布局重构：左侧会话抽屉（240px flex-none）+ 右侧聊天主区（单列气泡流 + 底部输入区）
2. 气泡形态决策已定：单列（用户右对齐 / AI 左对齐）+ 引用 snippet inline 折叠
3. 清理 M1 cosmetic 退化：`interactive-card` 5 处 / `reveal-list` 3 处 / `radial-gradient` 1 处 / `eyebrow=` 1 处 / 旧 token 类名 92 处
4. 消费 M1 原子组件（PageHeader / Card / Button / EmptyList）

**旧 token 类名替换映射**：
- `border-border-strong` / `border-border-muted` → `border`
- `text-text-muted` → `text-muted-foreground`
- `text-text-strong` → `text-foreground`
- `bg-accent` → `bg-brand-soft`
- `text-accent` → `text-brand`

## 必读 skills

- `superpowers:test-driven-development` — TDD 纪律
- `superpowers:verification-before-completion` — 完成前验证

## 期望输出

**代码变更**：
- Modify: `src/features/qa/*.tsx`（5 文件）
- Create: `tests/qa-page.test.tsx`

**commit**（SubAgent 完成后自行 commit，Conventional Commits 格式）

**回报格式**：
- `status` / `summary` / `concerns` / `files_changed` / `test_results`

## 验收标准

- [ ] `src/features/qa/**` grep `interactive-card` 命中 0
- [ ] `src/features/qa/**` grep `reveal-list` 命中 0
- [ ] `src/features/qa/**` grep `radial-gradient` 命中 0
- [ ] `src/features/qa/**` grep `eyebrow=` 命中 0
- [ ] `src/features/qa/**` grep `border-border-strong|text-text-muted|text-text-strong|border-border-muted|bg-accent[^-]|text-accent[^-]` 命中 0
- [ ] `tests/qa-page.test.tsx` PASS
- [ ] `corepack pnpm typecheck` 0 errors
- [ ] `corepack pnpm lint` 0 warnings/errors
- [ ] `corepack pnpm build` 成功
- [ ] git branch 仍是 `feature/001-webui-refactor`
- [ ] 已 commit

---

## 执行记录

- **DONE 时间**：2026-04-16 11:40
- **SubAgent 回报 status**：partial (DONE_WITH_CONCERNS)
- **commit**：`6d54ea7` — cleanup only (items 1-5 of Step 3)
- **完成项**：interactive-card 5 处 / reveal-list 3 处 / radial-gradient 1 处 / eyebrow= 1 处 / 旧 token 92 处 → grep 全 0 ✓
- **未完成项**：items 6-7（布局重整 sidebar+bubble + PageHeader 统一）
- **主 Agent review**：cleanup 验收通过（11/11 grep 验收 ✓），但布局重整是 T2.2 核心目的之一，未完成 → superseded，写 H4 fix
- **最终 status**：superseded（→ H4）
