# Handoff H2 · T2.1 implementer (practice page)

**派发时间**：2026-04-16 10:30
**状态**：completed
**派发关系**：主 Agent → implementer SubAgent
**关联**：H1-planning-phase（completed）；T2.1 task file

## 任务上下文（dev-workflow 注入，SubAgent 必读 + 严格遵守）

- **project_root**：`/root/.openclaw/projects/open-interview`
- **workstream_type**：`feature`
- **workstream_id**：`001-webui-refactor`
- **git_branch**：`feature/001-webui-refactor`
- **docs_dir**：`docs/001-webui-refactor/`
- **complexity**：`L2`
- **phase_dir**：`docs/001-webui-refactor/m2-feature-pages/`
- **work_dir**：`docs/001-webui-refactor/m2-feature-pages/`

**约束**：
- 代码改动限制在 `src/features/practice/*` + `tests/practice-page.test.tsx`
- **禁止修改** `src/components/*`、`src/app/*`、`src/features/workbench/*`（M1 领地）
- **禁止修改** 其他 feature 目录（`src/features/{qa,questions,review,interviews,import,resume}/*`）
- 所有视觉规范以 `docs/reference/design-system.md` 为唯一权威来源
- 遵循 TDD：先写失败测试 → run FAIL → 实现 → run PASS

## 任务描述

实现 T2.1：`/practice` 页面重构（灯塔页）。

**完整 task 规格见**：`docs/001-webui-refactor/m2-feature-pages/tasks/T2.1-practice-page.md`（必须 Read 完整，含轻量 design note 5 段 + 涉及文件 + 验收条件 + Step 1-5 TDD 流程）

**核心目标摘要**（详见 task file）：
1. 布局重构：顶部总览主卡 + 主舞台 + 右侧最近考试的三段布局
2. 雷达图决策：去 `radar-draw` animation，保留静态雷达图（默认选项 A）
3. 清理 M1 cosmetic 退化：`radar-draw` 1 处 / `reveal-list` 1 处 / 旧 token 类名 63 处
4. 消费 M1 原子组件（PageHeader / Card / CardHeader / CardBody / Button / EmptyList / DetailGrid / Badge）

**关键参考文件**（SubAgent 按需 Read）：
- `docs/reference/design-system.md` — 组件 API 和按钮摆放规则的唯一权威
- `docs/001-webui-refactor/m1-infra/report.md` §4 — M1 已知清理条目对照
- `src/features/practice/*.tsx` — 现有代码（11 文件）
- `src/components/ui/*.tsx` + `src/components/workbench/*.tsx` — M1 原子组件 API 参考（只读）

**旧 token 类名替换映射**（task file §Step 3 §3）：
- `border-border-strong` / `border-border-muted` → `border` (使用 Tailwind 默认 border-color，已由 M1 @layer base 规则映射到 `var(--color-border)`)
- `text-text-muted` → `text-muted-foreground`
- `text-text-strong` → `text-foreground`
- `bg-accent` → `bg-brand-soft`
- `text-accent` → `text-brand`

## 必读 skills

- `superpowers:test-driven-development` — TDD 纪律（Step 1-5）
- `superpowers:verification-before-completion` — 完成前验证

SubAgent 用 Skill 工具加载后再执行。

## 期望输出

**代码变更**：
- Modify: `src/features/practice/*.tsx`（最多 11 文件，见 task file §涉及文件）
- Create: `tests/practice-page.test.tsx`（废弃类名 grep-absent 冒烟测试）

**commit**（SubAgent 完成实现 + 测试通过后自行 commit）：
- 格式遵循 Conventional Commits
- commit message 中明示雷达图决策（保留静态 / 替换 KV）

**回报格式**：
- `status`: `DONE` / `DONE_WITH_CONCERNS` / `BLOCKED` / `NEEDS_CONTEXT`
- `summary`: 一句话
- `concerns`（如有）
- `files_changed`: 实际改动文件列表
- `test_results`: vitest / typecheck / lint / build 结果摘要

## 验收标准

（对应 task file §验收条件中的工具链可验证项）

- [ ] `src/features/practice/**` grep `radar-draw` 命中 0
- [ ] `src/features/practice/**` grep `reveal-list` 命中 0
- [ ] `src/features/practice/**` grep `border-border-strong|text-text-muted|text-text-strong|border-border-muted|bg-accent[^-]|text-accent[^-]` 命中 0
- [ ] `tests/practice-page.test.tsx` 存在且 PASS
- [ ] `corepack pnpm typecheck` 0 errors
- [ ] `corepack pnpm lint` 0 warnings/errors
- [ ] `corepack pnpm build` 成功
- [ ] git branch 仍是 `feature/001-webui-refactor`
- [ ] 已 commit（格式 `feat(ui): ...`）

---

## 执行记录

- **DONE 时间**：2026-04-16 11:15
- **SubAgent 回报 status**：DONE
- **commit**：`fb07583` — `feat(ui): refactor /practice page to three-segment layout, clear M1 cosmetic debt (M2/T2.1)`
- **files_changed**：10 files（9 modified + 1 created test），154 insertions / 114 deletions
- **雷达图决策**：Option A（保留静态，去 `radar-draw` animation）
- **验证结果**：vitest 4/4 PASS · typecheck 0 errors · lint 0 · build OK · grep banned-classes 0 hits
- **主 Agent review**：通过（9/9 工具链验收标准 ✓）
  - 三段布局 `lg:grid-cols-[3fr_2fr]` 确认
  - PageHeader Primary "开始训练" lg + EmptyList empty state 确认
  - Card/CardHeader/CardBody/EmptyList/Button 消费正确
  - 所有 banned class grep 0
- **最终 status**：completed
